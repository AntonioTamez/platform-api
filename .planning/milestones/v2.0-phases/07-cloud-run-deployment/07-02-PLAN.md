---
phase: 07-cloud-run-deployment
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false
requirements: [CLOUD-01]

user_setup:
  - service: google-cloud-platform
    why: "Deploy PersonsAPI container to Cloud Run; create project, billing, Artifact Registry, and the public service"
    env_vars:
      - name: PROJECT_ID
        source: "Chosen by user when running gcloud projects create (DEPLOYMENT.md Step 1)"
    dashboard_config:
      - task: "Create a GCP account and link a billing account (credit card; $300 free credit covers this project)"
        location: "https://console.cloud.google.com -> Billing"
      - task: "Create/store the persons-api-deployer service account JSON key for Phase 8 reuse"
        location: "Generated locally as key.json via DEPLOYMENT.md Step 5"

must_haves:
  truths:
    - "GET /health on the public Cloud Run HTTPS URL returns HTTP 200 from the public internet"
    - "GET /api/persons on the public URL returns the 3 seeded persons"
    - "The Cloud Run revision is serving with no crash loop (status conditions Ready=True)"
    - "Google Cloud Logging shows JSON log entries from the persons-api service"
  artifacts:
    - path: "DEPLOYMENT.md"
      provides: "The runbook the user executes; its verification commands produce the proof for this checkpoint"
      contains: "Step 8: Verify Deployment"
  key_links:
    - from: "Cloud Run service persons-api"
      to: "public HTTPS URL"
      via: "--allow-unauthenticated invoker binding to allUsers"
      pattern: "https://persons-api"
---

<objective>
Execute the Phase 7 runbook against the user's GCP account and verify CLOUD-01: the API is publicly reachable at a Google Cloud Run HTTPS URL. This plan confirms all 4 ROADMAP success criteria against the live service.

Purpose: Claude cannot create a GCP project, enter billing details (credit card), or deploy resources under the user's Google account — these require the human account holder. This plan is the human-action checkpoint that turns the DEPLOYMENT.md runbook into a verified live deployment.

Output: A confirmed public Cloud Run URL serving /health and /api/persons, a healthy revision, and JSON logs visible in Cloud Logging.
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-cloud-run-deployment/07-CONTEXT.md
@DEPLOYMENT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute the DEPLOYMENT.md runbook against your GCP account</name>
  <read_first>
    - DEPLOYMENT.md (the full runbook authored in Plan 07-01 — follow every step in order)
    - .planning/phases/07-cloud-run-deployment/07-CONTEXT.md (D-01 fresh project, D-05 scale-to-zero cold start, D-07 public access)
  </read_first>
  <what-built>
    DEPLOYMENT.md (from Plan 07-01) is a complete runbook to deploy the validated Phase 6 container image to Cloud Run. Claude has authored the runbook and the .gitignore key.json guard. This task is the manual execution that only the GCP account holder can perform (project creation, billing/credit-card entry, and resource deployment under your account).
  </what-built>
  <how-to-verify>
    1. Open DEPLOYMENT.md at the solution root.
    2. Install the gcloud SDK (Prerequisites) if not already installed, then `gcloud init` / `gcloud auth login`.
    3. Do a find-replace of `PROJECT_ID` with your actual project ID across the commands you paste.
    4. Execute Steps 1 through 7 in order: create project, link billing in the Console, enable APIs, create the Artifact Registry repo, configure Docker auth, create the service account + key.json, build/push the image, and `gcloud run deploy persons-api`.
    5. Confirm key.json was NOT staged by git: run `git status` and verify key.json does not appear (it is gitignored from Plan 07-01).
    6. Capture the printed Cloud Run service URL (or run the Step 8 describe command to get it).
  </how-to-verify>
  <action>
    The account holder runs the DEPLOYMENT.md runbook (Steps 1-7) to stand up the live service. Claude cannot perform GCP project creation, billing/credit-card entry, or deployment under the user's account. After deploy completes, paste the resulting Cloud Run URL (form `https://persons-api-<hash>-uc.a.run.app`) so the verification task can run against it.
  </action>
  <acceptance_criteria>
    - `gcloud run deploy persons-api` completed and printed a service URL of the form `https://persons-api-<hash>-uc.a.run.app`
    - `git status` does not list `key.json` (the credential is git-ignored)
    - The Cloud Run service `persons-api` exists in region `us-central1` (confirmable via `gcloud run services list --region us-central1`)
  </acceptance_criteria>
  <resume-signal>Paste the Cloud Run service URL and type "deployed" to continue to verification.</resume-signal>
  <done>The persons-api Cloud Run service is deployed in us-central1 with a public HTTPS URL; key.json is git-ignored, not staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 4 ROADMAP success criteria against the live URL</name>
  <read_first>
    - DEPLOYMENT.md (Step 8: Verify Deployment — the exact verification commands and expected outputs)
    - .planning/ROADMAP.md (Phase 7 Success Criteria — the 4 conditions that must be TRUE)
  </read_first>
  <what-built>
    A live, public Cloud Run deployment of persons-api (from Task 1). This task confirms the deployment satisfies CLOUD-01 by checking each of the 4 ROADMAP success criteria against the live HTTPS URL.
  </what-built>
  <how-to-verify>
    Using the URL captured in Task 1 (set `SERVICE_URL` to it):
    1. SC-1 (health, public internet, handles cold start): `curl --max-time 30 "$SERVICE_URL/health"` -> expect HTTP 200 with body `{"status":"Healthy"}`.
    2. SC-2 (seeded data): `curl "$SERVICE_URL/api/persons"` -> expect HTTP 200 with a JSON array of 3 persons.
    3. SC-3 (no crash loop): `gcloud run services describe persons-api --region us-central1 --format='value(status.conditions)'` -> Ready condition is True; no repeated container failures.
    4. SC-4 (logs): open `https://console.cloud.google.com/logs/query?project=PROJECT_ID` with filter `resource.type="cloud_run_revision" AND resource.labels.service_name="persons-api"` -> JSON log entries from the service are visible (DEFAULT severity is expected; colored icons are deferred per OBS-03).
  </how-to-verify>
  <action>
    The account holder runs the DEPLOYMENT.md Step 8 verification commands against the live URL and confirms each of the 4 success criteria. Report any criterion that fails (e.g., 503 on first curl indicates a cold-start timeout — retry once; a persistent 503 indicates a port mismatch, confirm the deploy used --port 8080 not --port 80).
  </action>
  <acceptance_criteria>
    - SC-1: `curl --max-time 30 "$SERVICE_URL/health"` returns HTTP 200 and body containing `"status":"Healthy"`
    - SC-2: `curl "$SERVICE_URL/api/persons"` returns HTTP 200 and a JSON array with exactly 3 person entries
    - SC-3: `gcloud run services describe persons-api --region us-central1 --format='value(status.conditions)'` shows Ready=True with no crash loop
    - SC-4: Cloud Logging Explorer (filtered to `resource.type="cloud_run_revision" AND resource.labels.service_name="persons-api"`) displays JSON-structured log entries from the running service
  </acceptance_criteria>
  <resume-signal>Type "verified" if all 4 criteria pass, or describe which criterion failed.</resume-signal>
  <done>All 4 ROADMAP Phase 7 success criteria pass against the live public Cloud Run URL — CLOUD-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> Cloud Run service | After deploy, allUsers can invoke all 6 API endpoints (read and write) over HTTPS |
| Developer machine -> git repository | key.json must remain unstaged during the manual deploy session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | key.json during manual deploy | mitigate | Task 1 acceptance requires `git status` to show key.json absent; .gitignore guard from Plan 07-01 enforces this |
| T-07-02-02 | Elevation of Privilege | Public unauthenticated endpoints | accept | Intentional per D-07; learning/demo project with seeded non-PII data; required to verify success criteria with plain curl |
| T-07-02-03 | Denial of Service | Scale-to-zero cold start on first request | accept | Per D-05; mitigated operationally by `curl --max-time 30` which holds for the ~2-4s container start; not a security exposure |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this phase — deployment + verification only |
</threat_model>

<verification>
- Task 1: persons-api deployed in us-central1 with a public URL; key.json not staged.
- Task 2: all 4 ROADMAP success criteria (health 200, 3 seeded persons, healthy revision, JSON logs) pass against the live URL.
</verification>

<success_criteria>
- The PersonsAPI is publicly reachable at a Google Cloud Run HTTPS URL (CLOUD-01).
- All 4 ROADMAP Phase 7 success criteria are confirmed against the live deployment.
- The service account key was created and preserved for Phase 8 without being committed to git.
</success_criteria>

<output>
Create `.planning/phases/07-cloud-run-deployment/07-02-SUMMARY.md` when done.
</output>
