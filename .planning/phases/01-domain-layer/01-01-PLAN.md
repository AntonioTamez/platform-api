---
phase: 01-domain-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PersonsAPI.sln
  - src/PersonsAPI.Domain/PersonsAPI.Domain.csproj
  - src/PersonsAPI.Domain/Exceptions/DomainException.cs
  - tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj
  - tests/PersonsAPI.Domain.Tests/DomainExceptionTests.cs
autonomous: true
requirements: [INFRA-02, VAL-02]
user_setup: []

must_haves:
  truths:
    - "D-03, D-04, D-05: DomainException is a sealed type inheriting directly from System.Exception (not ArgumentException) carrying an English message — the single exception type for all domain invariant violations"
    - "D-05: PersonsAPI.Domain compiles as a standalone .NET 10 class library with zero NuGet PackageReference entries (INFRA-02)"
    - "An xUnit test project exists, references the Domain project, and `dotnet test` runs green"
  artifacts:
    - path: "src/PersonsAPI.Domain/PersonsAPI.Domain.csproj"
      provides: "Zero-dependency Domain class library targeting net10.0, LangVersion 14, Nullable enable"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "src/PersonsAPI.Domain/Exceptions/DomainException.cs"
      provides: "Single custom domain exception type for all invariant violations"
      contains: "public sealed class DomainException : Exception"
    - path: "tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj"
      provides: "xUnit test harness referencing the Domain project"
      contains: "PersonsAPI.Domain.csproj"
    - path: "PersonsAPI.sln"
      provides: "Solution wiring both projects together"
      contains: "PersonsAPI.Domain"
  key_links:
    - from: "tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj"
      to: "src/PersonsAPI.Domain/PersonsAPI.Domain.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*PersonsAPI\\.Domain\\.csproj"
    - from: "tests/PersonsAPI.Domain.Tests/DomainExceptionTests.cs"
      to: "PersonsAPI.Domain.Exceptions.DomainException"
      via: "using + instantiation in test"
      pattern: "DomainException"
---

<objective>
Establish the solution skeleton: a zero-dependency `PersonsAPI.Domain` class library, the single `DomainException` type all domain invariants throw, and an xUnit test project wired to the Domain project. This is the foundation every later phase references and the test harness Plan 02 builds the Person entity against (test-first).

Purpose: Phase 1 success criterion #4 (INFRA-02) requires the Domain project to be provably isolated at the `.csproj` level — zero EF Core / framework PackageReference entries. The error contract (D-03/D-04/D-05, VAL-02) is the `DomainException` type. Both must exist and compile before the Person entity is written test-first in Plan 02, so the test project (the Nyquist automated-verification harness) must exist first.
Output: `PersonsAPI.sln`, `src/PersonsAPI.Domain/PersonsAPI.Domain.csproj`, `src/PersonsAPI.Domain/Exceptions/DomainException.cs`, `tests/PersonsAPI.Domain.Tests/` xUnit project with one passing DomainException test.
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-domain-layer/01-CONTEXT.md
@.planning/phases/01-domain-layer/01-RESEARCH.md
@.planning/phases/01-domain-layer/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold solution and zero-dependency Domain library</name>
  <files>PersonsAPI.sln, src/PersonsAPI.Domain/PersonsAPI.Domain.csproj</files>
  <read_first>CLAUDE.md, .planning/phases/01-domain-layer/01-RESEARCH.md, .planning/phases/01-domain-layer/01-PATTERNS.md</read_first>
  <action>From the repository root (C:/ATS/Git/platform), create the solution and the Domain class library. Run `dotnet new sln --name PersonsAPI` to produce `PersonsAPI.sln`. Run `dotnet new classlib --name PersonsAPI.Domain --output src/PersonsAPI.Domain --framework net10.0`, then delete the template-generated `src/PersonsAPI.Domain/Class1.cs` placeholder. Add the project to the solution with `dotnet sln PersonsAPI.sln add src/PersonsAPI.Domain/PersonsAPI.Domain.csproj`. Edit `src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` so its single `<PropertyGroup>` contains exactly these four child elements: `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`, `<LangVersion>14</LangVersion>`. The csproj MUST contain zero `<PackageReference>` elements and zero `<ProjectReference>` elements — this structurally enforces INFRA-02 (Domain is the innermost layer with no outbound dependencies). Do NOT add Microsoft.EntityFrameworkCore, FluentValidation, or any other package. Use file paths under `src/` per the RESEARCH.md recommended structure.</action>
  <verify>
    <automated>dotnet build src/PersonsAPI.Domain/PersonsAPI.Domain.csproj -c Debug 2>&1 | grep -E "Build succeeded|0 Error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` contains "<TargetFramework>net10.0</TargetFramework>", "<Nullable>enable</Nullable>", "<ImplicitUsings>enable</ImplicitUsings>", and "<LangVersion>14</LangVersion>"
    - `grep -c "<PackageReference" src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` returns 0
    - `grep -c "<ProjectReference" src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` returns 0
    - `PersonsAPI.sln` exists at repo root and contains the string "PersonsAPI.Domain"
    - `src/PersonsAPI.Domain/Class1.cs` does NOT exist
    - `dotnet build src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` reports "Build succeeded" with 0 errors
  </acceptance_criteria>
  <done>Domain class library compiles, is registered in the solution, and its csproj has zero package and zero project references.</done>
</task>

<task type="auto">
  <name>Task 2: Add the DomainException error contract</name>
  <files>src/PersonsAPI.Domain/Exceptions/DomainException.cs</files>
  <read_first>src/PersonsAPI.Domain/PersonsAPI.Domain.csproj, .planning/phases/01-domain-layer/01-PATTERNS.md, .planning/phases/01-domain-layer/01-CONTEXT.md</read_first>
  <action>Create `src/PersonsAPI.Domain/Exceptions/DomainException.cs` using a file-scoped namespace `PersonsAPI.Domain.Exceptions`. Declare `public sealed class DomainException : Exception` — it MUST inherit directly from `System.Exception`, NOT from `ArgumentException` or any other BCL subclass (per D-05 and RESEARCH.md Pitfall 5: inheriting from ArgumentException would conflate programming errors with domain-rule violations and break the Application layer's type-based catch in Phase 2). Provide exactly two constructors: one taking `(string message)` calling `: base(message)`, and one taking `(string message, Exception innerException)` calling `: base(message, innerException)`. Add no additional properties — the message string carries all violation detail (D-05). Add a `/// <summary>` XML doc comment on the class stating it is thrown when a domain invariant is violated, the message describes the rule violation in plain English, and the Application layer catches it by type. All identifiers, comments, and docs in English (CLAUDE.md constraint, D-04).</action>
  <verify>
    <automated>dotnet build src/PersonsAPI.Domain/PersonsAPI.Domain.csproj -c Debug 2>&1 | grep -E "Build succeeded|0 Error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Domain/Exceptions/DomainException.cs` contains "namespace PersonsAPI.Domain.Exceptions"
    - File contains "public sealed class DomainException : Exception"
    - `grep -c "ArgumentException" src/PersonsAPI.Domain/Exceptions/DomainException.cs` returns 0
    - File contains a constructor signature `DomainException(string message)` and `DomainException(string message, Exception innerException)`
    - File contains a `/// <summary>` doc comment
    - `dotnet build src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` reports "Build succeeded"
  </acceptance_criteria>
  <done>DomainException compiles, is sealed, inherits directly from Exception, and exposes the message-only and message+inner constructors.</done>
</task>

<task type="auto">
  <name>Task 3: Create xUnit test project and verify DomainException behavior</name>
  <files>tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj, tests/PersonsAPI.Domain.Tests/DomainExceptionTests.cs</files>
  <read_first>src/PersonsAPI.Domain/Exceptions/DomainException.cs, PersonsAPI.sln</read_first>
  <action>Create the test project with `dotnet new xunit --name PersonsAPI.Domain.Tests --output tests/PersonsAPI.Domain.Tests --framework net10.0` (the first-party SDK template; it pins xunit 2.9.x, Microsoft.NET.Test.Sdk 17.x, xunit.runner.visualstudio 3.x, coverlet.collector 6.x — these are the ONLY packages introduced and they come solely from the official template, no hand-added packages). Delete the template-generated `tests/PersonsAPI.Domain.Tests/UnitTest1.cs`. Add a project reference to Domain: `dotnet add tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj reference src/PersonsAPI.Domain/PersonsAPI.Domain.csproj`. Register the test project in the solution: `dotnet sln PersonsAPI.sln add tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj`. Create `tests/PersonsAPI.Domain.Tests/DomainExceptionTests.cs` (file-scoped namespace `PersonsAPI.Domain.Tests`, `using PersonsAPI.Domain.Exceptions;`) with two `[Fact]` tests: (1) `Constructor_WithMessage_SetsMessage` — `new DomainException("rule broken")` has `.Message == "rule broken"`; (2) `DomainException_InheritsDirectlyFromException` — assert `typeof(DomainException).BaseType == typeof(Exception)` (this guards the D-05 inheritance invariant so a regression to `: ArgumentException` fails the build). This is the Nyquist test scaffold that Plan 02 extends with Person tests.</action>
  <verify>
    <automated>dotnet test tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj 2>&1 | grep -E "Passed!|Passed:[[:space:]]*2"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj` contains a ProjectReference whose path includes "PersonsAPI.Domain.csproj"
    - `tests/PersonsAPI.Domain.Tests/UnitTest1.cs` does NOT exist
    - `DomainExceptionTests.cs` contains "typeof(DomainException).BaseType == typeof(Exception)"
    - `PersonsAPI.sln` contains the string "PersonsAPI.Domain.Tests"
    - `dotnet test` reports "Passed!" with 2 passing tests and 0 failures
  </acceptance_criteria>
  <done>The solution contains a Domain project and a test project; `dotnet test` runs green with the two DomainException facts passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → solution | NuGet restore pulls test-project packages from the official `dotnet new xunit` template only |
| (none for runtime) | Domain library has no HTTP, network, persistence, or external-input surface in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DomainException base type | mitigate | Test `DomainException_InheritsDirectlyFromException` asserts `BaseType == typeof(Exception)`; a regression to `: ArgumentException` (which would let the Phase 2 catch swallow unrelated BCL errors) fails the build |
| T-01-02 | Information disclosure | DomainException message | accept | Messages describe business-rule violations in plain English (D-04) and carry no secrets, stack internals, or PII; acceptable for a learning project with no auth/PII surface |
| T-01-03 | Tampering | Domain .csproj dependency surface | mitigate | INFRA-02 enforced: csproj has zero `<PackageReference>`/`<ProjectReference>`; grep-count gate `== 0` prevents EF/framework leakage into the domain core |
| T-01-SC | Tampering | NuGet supply chain (test project) | mitigate | All test packages originate solely from the first-party `dotnet new xunit` template (Microsoft.NET.Test.Sdk, xunit, xunit.runner.visualstudio, coverlet.collector); no hand-added or `[ASSUMED]`/`[SUS]` packages — RESEARCH.md Package Legitimacy Audit confirms zero net-new package selection |

Residual risk: T-01-02 accepted (no sensitive data in domain layer). No high-severity threats — security_block_on=high is satisfied.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `dotnet build PersonsAPI.sln -c Debug` reports Build succeeded with 0 errors
- [ ] `grep -c "<PackageReference" src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` returns 0
- [ ] `grep -c "<ProjectReference" src/PersonsAPI.Domain/PersonsAPI.Domain.csproj` returns 0
- [ ] `dotnet test tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj` reports Passed! (2 tests)
</verification>

<success_criteria>
- All three tasks completed
- Domain class library compiles with zero package/project references (INFRA-02)
- DomainException is sealed, inherits directly from Exception, English message (VAL-02 error contract)
- xUnit test project references Domain and `dotnet test` is green
- Solution wires both projects
</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-layer/01-01-SUMMARY.md`
</output>
