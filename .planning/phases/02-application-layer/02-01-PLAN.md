---
phase: 02-application-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PersonsAPI.sln
  - src/PersonsAPI.Application/PersonsAPI.Application.csproj
  - src/PersonsAPI.Application/IApplicationMarker.cs
  - src/PersonsAPI.Application/Ports/IPersonRepository.cs
  - src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs
  - src/PersonsAPI.Application/DTOs/PersonResponse.cs
  - src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs
  - src/PersonsAPI.Application/DTOs/UpdatePersonRequest.cs
  - src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs
  - tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj
  - tests/PersonsAPI.Application.Tests/DTOs/PersonResponseTests.cs
autonomous: true
requirements: [INFRA-03]
user_setup: []

must_haves:
  truths:
    - "PersonsAPI.Application compiles as a standalone .NET 10 class library against net10.0 with Mediator.Abstractions 3.0.2, FluentValidation 12.1.1, and FluentValidation.DependencyInjectionExtensions 12.1.1 — and zero Mediator.SourceGenerator, EF Core, or ASP.NET Core PackageReference entries"
    - "INFRA-03: IPersonRepository is declared in src/PersonsAPI.Application/Ports/IPersonRepository.cs with five Task-returning CRUD methods — the port lives in Application, not Infrastructure"
    - "D-03: IPersonRepository.GetByIdAsync returns Person? (nullable) so the Application layer (not the repository) decides what 'not found' means"
    - "D-04: PersonNotFoundException is a sealed Exception subclass in PersonsAPI.Application.Exceptions exposing an int PersonId property"
    - "D-05: Three distinct request DTO records exist — CreatePersonRequest and UpdatePersonRequest with non-nullable fields, UpdatePersonDto with all-nullable fields for PATCH"
    - "D-06, D-07: PersonResponse is a record with an Age field populated by a static FromDomain(Person) factory that reads person.Age (the domain's computed property) — no AutoMapper anywhere in the assembly"
    - "An xUnit test project PersonsAPI.Application.Tests is wired to the Application project and runs green, covering PersonResponse.FromDomain"
  artifacts:
    - path: "src/PersonsAPI.Application/PersonsAPI.Application.csproj"
      provides: "Application class library targeting net10.0 with LangVersion 14, Nullable enable, ProjectReference to Domain, and three PackageReferences (Mediator.Abstractions 3.0.2, FluentValidation 12.1.1, FluentValidation.DependencyInjectionExtensions 12.1.1)"
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "src/PersonsAPI.Application/IApplicationMarker.cs"
      provides: "Empty marker interface used as typeof(IApplicationMarker).Assembly anchor for FluentValidation assembly scanning"
      contains: "public interface IApplicationMarker"
    - path: "src/PersonsAPI.Application/Ports/IPersonRepository.cs"
      provides: "Secondary port interface for Person persistence — five Task-returning CRUD methods"
      contains: "public interface IPersonRepository"
    - path: "src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs"
      provides: "sealed Exception subclass thrown by Application handlers when GetByIdAsync returns null"
      contains: "public sealed class PersonNotFoundException : Exception"
    - path: "src/PersonsAPI.Application/DTOs/PersonResponse.cs"
      provides: "Response record with Id, FirstName, PaternalLastName, MaternalLastName, DateOfBirth, Age and static FromDomain(Person) factory"
      contains: "public static PersonResponse FromDomain(Person person)"
    - path: "src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs"
      provides: "Request record with four non-nullable fields for POST"
      contains: "public record CreatePersonRequest"
    - path: "src/PersonsAPI.Application/DTOs/UpdatePersonRequest.cs"
      provides: "Request record with four non-nullable fields for PUT"
      contains: "public record UpdatePersonRequest"
    - path: "src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs"
      provides: "Request record with four nullable fields for PATCH"
      contains: "public record UpdatePersonDto"
    - path: "tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj"
      provides: "xUnit test harness referencing Application + Domain projects"
      contains: "PersonsAPI.Application.csproj"
    - path: "tests/PersonsAPI.Application.Tests/DTOs/PersonResponseTests.cs"
      provides: "Test class proving PersonResponse.FromDomain maps every field including computed Age"
      contains: "PersonResponse.FromDomain"
  key_links:
    - from: "src/PersonsAPI.Application/PersonsAPI.Application.csproj"
      to: "src/PersonsAPI.Domain/PersonsAPI.Domain.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*PersonsAPI\\.Domain\\.csproj"
    - from: "src/PersonsAPI.Application/Ports/IPersonRepository.cs"
      to: "PersonsAPI.Domain.Entities.Person"
      via: "using PersonsAPI.Domain.Entities"
      pattern: "using PersonsAPI\\.Domain\\.Entities"
    - from: "src/PersonsAPI.Application/DTOs/PersonResponse.cs"
      to: "PersonsAPI.Domain.Entities.Person.Age"
      via: "static FromDomain factory reads person.Age"
      pattern: "person\\.Age"
    - from: "tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj"
      to: "src/PersonsAPI.Application/PersonsAPI.Application.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*PersonsAPI\\.Application\\.csproj"
---

<objective>
Establish the Application-layer foundation per INFRA-03: a standalone PersonsAPI.Application class library, the secondary port interface (IPersonRepository), the PersonNotFoundException type (D-03/D-04), the four DTO records (D-05/D-06), the static PersonResponse.FromDomain factory (D-07), and an xUnit test project that proves the assembly builds and that mapping covers the computed Age. Every later plan in this phase — and the Infrastructure layer in Phase 3 — references the artifacts created here.

Purpose: Phase 2 success criterion #1 ("IPersonRepository is declared in PersonsAPI.Application — no reference to it exists in Domain or Infrastructure at definition time") and criterion #4 ("All command and query records, DTOs, and request types compile with references only to Domain and Mediator.Abstractions — no Infrastructure or ASP.NET Core types appear in Application") both demand a compilable Application project with the exact package set. The DTOs and exception type are the building blocks every CQRS handler in Plan 02 imports. The test project (the Nyquist automated-verification harness for this phase) must exist now so Plans 02 and 03 can land test cases alongside their code.
Output: src/PersonsAPI.Application/* project tree (csproj + marker + Ports/ + Exceptions/ + DTOs/) plus tests/PersonsAPI.Application.Tests/ wired into PersonsAPI.sln; `dotnet build` and `dotnet test` both pass.
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-application-layer/02-CONTEXT.md
@.planning/phases/02-application-layer/02-RESEARCH.md
@.planning/phases/02-application-layer/02-PATTERNS.md
@src/PersonsAPI.Domain/Entities/Person.cs
@src/PersonsAPI.Domain/Exceptions/DomainException.cs
@src/PersonsAPI.Domain/PersonsAPI.Domain.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold PersonsAPI.Application class library, add packages, and wire test project into the solution</name>
  <files>PersonsAPI.sln, src/PersonsAPI.Application/PersonsAPI.Application.csproj, tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj</files>
  <read_first>
    - CLAUDE.md (per D-04 stack section: Mediator.Abstractions 3.0.2, FluentValidation 12.1.1, FluentValidation.DependencyInjectionExtensions 12.1.1; no Mediator.SourceGenerator in Application)
    - .planning/phases/02-application-layer/02-RESEARCH.md (Standard Stack — exact versions and install commands; Pitfall 1 — do NOT add Mediator.SourceGenerator here)
    - .planning/phases/02-application-layer/02-PATTERNS.md (csproj base pattern — copy PropertyGroup verbatim from Domain csproj; PackageReference additions block)
    - src/PersonsAPI.Domain/PersonsAPI.Domain.csproj (current PropertyGroup template to mirror: net10.0, Nullable enable, ImplicitUsings enable, LangVersion 14)
    - tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj (existing test project shape: xUnit 2.9.3, Microsoft.NET.Test.Sdk 17.14.1, xunit.runner.visualstudio 3.1.4, coverlet.collector 6.0.4, IsPackable false, ProjectReference to Domain)
    - PersonsAPI.sln (current solution structure with src and tests solution folders to preserve)
  </read_first>
  <action>
From the repository root C:/ATS/Git/platform, scaffold the Application class library and the Application test project, then wire both into PersonsAPI.sln per D-04 (stack) and PATTERNS.md "Csproj base pattern".

Step A — Create the Application class library:
- Run `dotnet new classlib --name PersonsAPI.Application --output src/PersonsAPI.Application --framework net10.0`.
- Delete the template-generated `src/PersonsAPI.Application/Class1.cs` placeholder.
- Edit `src/PersonsAPI.Application/PersonsAPI.Application.csproj` so the single `<PropertyGroup>` contains exactly four child elements (mirror Domain csproj): `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`, `<LangVersion>14</LangVersion>`.
- Add the ProjectReference to Domain: `dotnet add src/PersonsAPI.Application/PersonsAPI.Application.csproj reference src/PersonsAPI.Domain/PersonsAPI.Domain.csproj`.
- Add three PackageReferences via CLI (exact versions from RESEARCH.md Standard Stack):
  - `dotnet add src/PersonsAPI.Application/PersonsAPI.Application.csproj package Mediator.Abstractions --version 3.0.2`
  - `dotnet add src/PersonsAPI.Application/PersonsAPI.Application.csproj package FluentValidation --version 12.1.1`
  - `dotnet add src/PersonsAPI.Application/PersonsAPI.Application.csproj package FluentValidation.DependencyInjectionExtensions --version 12.1.1`
- Do NOT add `Mediator.SourceGenerator` — it belongs only in the Api project in Phase 4 (RESEARCH.md Pitfall 1, github.com/martinothamar/Mediator issue #261). Do NOT add `Microsoft.EntityFrameworkCore.*` or any `Microsoft.AspNetCore.*` package (D-01, INFRA-02 spirit).
- Add the project to the solution: `dotnet sln PersonsAPI.sln add src/PersonsAPI.Application/PersonsAPI.Application.csproj`. The CLI inserts it under the existing `src` solution folder GUID `{827E0CD3-B72D-47B6-A68D-7590B98EB39B}`.

Step B — Create the Application test project:
- Run `dotnet new xunit --name PersonsAPI.Application.Tests --output tests/PersonsAPI.Application.Tests --framework net10.0`.
- Delete the template-generated `tests/PersonsAPI.Application.Tests/UnitTest1.cs` placeholder.
- Edit `tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` to match the shape of `tests/PersonsAPI.Domain.Tests/PersonsAPI.Domain.Tests.csproj`: target framework `net10.0`, `Nullable enable`, `ImplicitUsings enable`, `IsPackable false`. PackageReference block must contain xunit 2.9.3, xunit.runner.visualstudio 3.1.4, Microsoft.NET.Test.Sdk 17.14.1, coverlet.collector 6.0.4. Include the `<Using Include="Xunit" />` block from the existing Domain.Tests csproj.
- Add ProjectReferences: `dotnet add tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj reference src/PersonsAPI.Application/PersonsAPI.Application.csproj` and `dotnet add tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj reference src/PersonsAPI.Domain/PersonsAPI.Domain.csproj`.
- Add to solution: `dotnet sln PersonsAPI.sln add tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` (CLI places it under the existing `tests` solution folder GUID `{0AB3BF05-4346-4AA6-1389-037BE0695223}`).

Step C — Verify both projects compile against the rest of the solution by running `dotnet build PersonsAPI.sln -c Debug`. Build must succeed with zero errors.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet build PersonsAPI.sln -c Debug 2>&1 | grep -E "Build succeeded|0 Error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/PersonsAPI.Application.csproj` exists and contains exactly these four PropertyGroup child elements: "<TargetFramework>net10.0</TargetFramework>", "<Nullable>enable</Nullable>", "<ImplicitUsings>enable</ImplicitUsings>", "<LangVersion>14</LangVersion>"
    - `grep -c "Mediator.Abstractions" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns >= 1 with Version "3.0.2"
    - `grep -c "FluentValidation\"" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns >= 1 with Version "12.1.1"
    - `grep -c "FluentValidation.DependencyInjectionExtensions" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns >= 1 with Version "12.1.1"
    - `grep -c "Mediator.SourceGenerator" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns 0
    - `grep -c "Microsoft.EntityFrameworkCore" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns 0
    - `grep -c "Microsoft.AspNetCore" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns 0
    - `src/PersonsAPI.Application/PersonsAPI.Application.csproj` contains a `<ProjectReference Include="..\PersonsAPI.Domain\PersonsAPI.Domain.csproj" />`
    - `tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` exists, contains ProjectReference to `PersonsAPI.Application.csproj`, and contains xunit 2.9.3
    - PersonsAPI.sln contains both `PersonsAPI.Application` and `PersonsAPI.Application.Tests` project entries
    - `dotnet build PersonsAPI.sln -c Debug` exits 0 and prints "Build succeeded"
    - Both Class1.cs and UnitTest1.cs template files have been deleted
  </acceptance_criteria>
  <done>The Application class library and its test project exist with the exact package set (no source generator, no EF Core, no ASP.NET Core), are referenced from PersonsAPI.sln, both reference Domain, and `dotnet build PersonsAPI.sln` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create marker interface, IPersonRepository port (INFRA-03, D-03), and PersonNotFoundException (D-04)</name>
  <files>src/PersonsAPI.Application/IApplicationMarker.cs, src/PersonsAPI.Application/Ports/IPersonRepository.cs, src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs</files>
  <read_first>
    - src/PersonsAPI.Application/PersonsAPI.Application.csproj (confirm packages are present from Task 1)
    - src/PersonsAPI.Domain/Entities/Person.cs (Person type that IPersonRepository operates on; namespace PersonsAPI.Domain.Entities)
    - src/PersonsAPI.Domain/Exceptions/DomainException.cs (existing sealed exception pattern to mirror — two constructors, English message, file-scoped namespace, sealed class)
    - .planning/phases/02-application-layer/02-CONTEXT.md (D-03 IPersonRepository nullable contract; D-04 PersonNotFoundException home)
    - .planning/phases/02-application-layer/02-PATTERNS.md (Pattern Assignments for IPersonRepository.cs and PersonNotFoundException.cs — full canonical signatures and XML doc style)
    - .planning/phases/02-application-layer/02-RESEARCH.md (IPersonRepository Method Signatures canonical reference; Pattern 4 rationale — Task<T> not ValueTask<T> for EF Core compatibility)
  </read_first>
  <action>
Create three foundational types using file-scoped namespaces, sealed classes, and `/// <summary>` XML docs that mirror PATTERNS.md "Shared Patterns" (Phase 1 style).

File 1 — `src/PersonsAPI.Application/IApplicationMarker.cs`:
- Namespace `PersonsAPI.Application`.
- Public empty interface `IApplicationMarker { }` with a single `/// <summary>` describing its sole purpose: anchor for `typeof(IApplicationMarker).Assembly` used by `AddValidatorsFromAssembly` in Plan 03's `AddApplication()` extension.
- No members, no base interfaces.

File 2 — `src/PersonsAPI.Application/Ports/IPersonRepository.cs`:
- Namespace `PersonsAPI.Application.Ports`.
- `using PersonsAPI.Domain.Entities;` for the Person type.
- Public interface `IPersonRepository` with exactly these five methods, each with a `CancellationToken cancellationToken = default` parameter and an XML doc summary copied verbatim from PATTERNS.md Pattern Assignment for IPersonRepository.cs:
  - `Task<IReadOnlyList<Person>> GetAllAsync(CancellationToken cancellationToken = default);`
  - `Task<Person?> GetByIdAsync(int id, CancellationToken cancellationToken = default);`  (nullable return per D-03)
  - `Task AddAsync(Person person, CancellationToken cancellationToken = default);`
  - `Task UpdateAsync(Person person, CancellationToken cancellationToken = default);`
  - `Task DeleteAsync(Person person, CancellationToken cancellationToken = default);`
- All five methods return `Task<T>` (NOT `ValueTask<T>`) so Phase 3's EF Core adapter returns its native Task types without wrapping (RESEARCH.md Pattern 4 + assumption A1).
- `UpdateAsync` and `DeleteAsync` take the Person entity (not just an id) — handlers fetch first, repository persists (RESEARCH.md A2; standard EF Core tracked-entity pattern).
- Do NOT introduce a generic `IRepository<T>` base — this is a specific typed port (CLAUDE.md "What NOT to Use"; D-04 stack).

File 3 — `src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs`:
- Namespace `PersonsAPI.Application.Exceptions`.
- `public sealed class PersonNotFoundException : Exception` (inherits directly from `System.Exception`, NOT from `KeyNotFoundException` and NOT from `PersonsAPI.Domain.Exceptions.DomainException` — D-04 names it an Application-layer concern, not a domain invariant).
- Public read-only property `int PersonId { get; }` so Phase 4 can extract the id for Problem Details without parsing the message string.
- Two constructors mirroring `DomainException.cs` style:
  - `public PersonNotFoundException(int id) : base($"Person with ID {id} was not found.") { PersonId = id; }`
  - `public PersonNotFoundException(int id, Exception innerException) : base($"Person with ID {id} was not found.", innerException) { PersonId = id; }`
- XML doc summary stating it is thrown by Application handlers when `GetByIdAsync` returns null and caught by Phase 4 to produce 404 Problem Details.

After authoring all three files, run `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` and confirm it succeeds with zero errors and zero warnings.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug 2>&1 | grep -E "Build succeeded|0 Error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/IApplicationMarker.cs` contains "namespace PersonsAPI.Application;" and "public interface IApplicationMarker"
    - `src/PersonsAPI.Application/Ports/IPersonRepository.cs` contains "namespace PersonsAPI.Application.Ports;" and "public interface IPersonRepository"
    - The file declares exactly these five method signatures (verified by grep): `Task<IReadOnlyList<Person>> GetAllAsync`, `Task<Person?> GetByIdAsync`, `Task AddAsync`, `Task UpdateAsync`, `Task DeleteAsync`
    - `grep -c "ValueTask" src/PersonsAPI.Application/Ports/IPersonRepository.cs` returns 0
    - `grep -c "Task<Person?>" src/PersonsAPI.Application/Ports/IPersonRepository.cs` returns >= 1 (D-03 nullable contract)
    - `grep -c "IRepository<" src/PersonsAPI.Application/Ports/IPersonRepository.cs` returns 0 (no generic base)
    - `src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs` contains "namespace PersonsAPI.Application.Exceptions;" and "public sealed class PersonNotFoundException : Exception"
    - That same file contains "public int PersonId { get; }" and two constructors taking `int id` (one with Exception innerException)
    - `grep -c "DomainException" src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs` returns 0 (PersonNotFoundException does NOT inherit from DomainException — D-04)
    - `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` exits 0 and prints "Build succeeded"
  </acceptance_criteria>
  <done>The marker interface, the IPersonRepository port (five Task-returning methods with nullable GetByIdAsync), and PersonNotFoundException (sealed, with PersonId property) all compile. INFRA-03 is satisfied at the file level — the port lives in PersonsAPI.Application.Ports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author four DTO records (D-05, D-06) with PersonResponse.FromDomain factory (D-07) and a test that proves Age mapping</name>
  <files>src/PersonsAPI.Application/DTOs/PersonResponse.cs, src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs, src/PersonsAPI.Application/DTOs/UpdatePersonRequest.cs, src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs, tests/PersonsAPI.Application.Tests/DTOs/PersonResponseTests.cs</files>
  <behavior>
    - Given a domain Person built via Person.Create("Ada", "Lovelace", "Byron", DateOnly born 30 years ago), PersonResponse.FromDomain returns a PersonResponse whose Id, FirstName, PaternalLastName, MaternalLastName, and DateOfBirth equal the entity's fields, and whose Age equals 30 (matching person.Age — the computed domain property, not a stored field) — D-06, D-07.
    - PersonResponse is a record (record equality test: two PersonResponse instances built from the same field values are equal).
    - CreatePersonRequest is a record with four non-nullable positional parameters in the order FirstName, PaternalLastName, MaternalLastName, DateOfBirth (D-05).
    - UpdatePersonRequest is a record with the same four non-nullable positional parameters (D-05 — identical shape, distinct type).
    - UpdatePersonDto is a record with four nullable positional parameters: string?, string?, string?, DateOnly? in the order FirstName, PaternalLastName, MaternalLastName, DateOfBirth (D-02, D-05).
  </behavior>
  <read_first>
    - src/PersonsAPI.Domain/Entities/Person.cs (Person.Create signature, Age computed property formula — month/day-aware DateOnly subtraction)
    - .planning/phases/02-application-layer/02-PATTERNS.md (Pattern Assignments for PersonResponse.cs, CreatePersonRequest.cs, UpdatePersonRequest.cs, UpdatePersonDto.cs — all four canonical records)
    - .planning/phases/02-application-layer/02-CONTEXT.md (D-02, D-05, D-06, D-07 decisions)
    - tests/PersonsAPI.Domain.Tests/PersonTests.cs (xUnit style baseline — using [Fact], [Theory], Assert.Equal patterns to mirror)
  </read_first>
  <action>
RED→GREEN sequence per CLAUDE.md TDD discipline: write the failing test first, then author the four DTO records.

Step RED — `tests/PersonsAPI.Application.Tests/DTOs/PersonResponseTests.cs`:
- Namespace `PersonsAPI.Application.Tests.DTOs`.
- usings: `PersonsAPI.Application.DTOs;`, `PersonsAPI.Domain.Entities;` (Xunit comes from ImplicitUsings via the csproj `<Using Include="Xunit" />`).
- xUnit `[Fact] FromDomain_MapsAllFieldsIncludingComputedAge`: build a Person via `Person.Create("Ada", "Lovelace", "Byron", DateOnly.FromDateTime(DateTime.Today.AddYears(-30)))`, call `PersonResponse.FromDomain(person)`, then assert each field individually with `Assert.Equal` — especially `Assert.Equal(person.Age, response.Age)` AND `Assert.Equal(30, response.Age)` to prove the computed value flows through.
- xUnit `[Fact] PersonResponse_RecordEquality_HoldsByValue`: build two PersonResponse instances with the same positional arguments and assert `Assert.Equal(a, b)` (proves the type is a record, not a class).
- Run `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` — it must fail to compile with CS0246 (PersonResponse not found). Confirm RED before continuing.

Step GREEN — author the four DTO record files (file-scoped namespaces, `/// <summary>` docs from PATTERNS.md):

File 1 — `src/PersonsAPI.Application/DTOs/PersonResponse.cs`:
- Namespace `PersonsAPI.Application.DTOs`.
- `using PersonsAPI.Domain.Entities;`.
- `public record PersonResponse(int Id, string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth, int Age)` — six positional parameters in this exact order, matching CONTEXT.md D-06.
- Body contains exactly one member: `public static PersonResponse FromDomain(Person person) => new(person.Id, person.FirstName, person.PaternalLastName, person.MaternalLastName, person.DateOfBirth, person.Age);` — D-07.
- XML doc summary on the record AND on FromDomain noting "Age is read from the domain entity's computed property — never stored" (D-06).

File 2 — `src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs`:
- Namespace `PersonsAPI.Application.DTOs`.
- `public record CreatePersonRequest(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth);` — four non-nullable positional parameters (D-05).
- XML doc summary: "Request body for POST /api/persons. All four fields are required."

File 3 — `src/PersonsAPI.Application/DTOs/UpdatePersonRequest.cs`:
- Namespace `PersonsAPI.Application.DTOs`.
- `public record UpdatePersonRequest(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth);` — four non-nullable positional parameters (D-05). Identical shape to CreatePersonRequest; distinct type by intent — do NOT merge or alias them.
- XML doc summary: "Request body for PUT /api/persons/{id}. All four fields are required (full replacement)."

File 4 — `src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs`:
- Namespace `PersonsAPI.Application.DTOs`.
- `public record UpdatePersonDto(string? FirstName, string? PaternalLastName, string? MaternalLastName, DateOnly? DateOfBirth);` — four nullable positional parameters (D-02, D-05). Parameter order MUST be FirstName, PaternalLastName, MaternalLastName, DateOfBirth (this is the order PATCH handlers and Plan 02's PatchPersonHandler rely on). Do NOT import `Microsoft.AspNetCore.JsonPatch` here — D-01 keeps ASP.NET Core types out of Application.
- XML doc summary: "DTO for PATCH. Null fields are not applied to the domain entity."

Step GREEN VERIFY — run `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` and confirm both tests pass (exit code 0).
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj --no-restore 2>&1 | grep -E "Passed!|Passed:.*Failed: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/DTOs/PersonResponse.cs` contains "public record PersonResponse(" with six positional parameters in order Id, FirstName, PaternalLastName, MaternalLastName, DateOfBirth, Age
    - The same file contains `public static PersonResponse FromDomain(Person person)` and the factory body references `person.Age` (verified by `grep -c "person.Age" src/PersonsAPI.Application/DTOs/PersonResponse.cs` returning >= 1)
    - `src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs` contains "public record CreatePersonRequest(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth)"
    - `src/PersonsAPI.Application/DTOs/UpdatePersonRequest.cs` contains "public record UpdatePersonRequest(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth)"
    - `src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs` contains "public record UpdatePersonDto(string? FirstName, string? PaternalLastName, string? MaternalLastName, DateOnly? DateOfBirth)"
    - `grep -c "JsonPatch" src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs` returns 0
    - `grep -c "Microsoft.AspNetCore" src/PersonsAPI.Application/` (recursive on .cs files) returns 0
    - `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` exits 0 and reports "Passed: 2, Failed: 0"
    - `PersonResponseTests.FromDomain_MapsAllFieldsIncludingComputedAge` passes (proves Age mapping)
    - `PersonResponseTests.PersonResponse_RecordEquality_HoldsByValue` passes (proves record semantics)
  </acceptance_criteria>
  <done>Four DTO records exist with correct types/nullability per D-02/D-05/D-06; PersonResponse.FromDomain maps every field including the computed Age; the test project compiles and both DTO tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application port → external adapter | IPersonRepository is the contract Infrastructure (Phase 3) implements. The Application layer trusts only this interface; no other adapter surface bleeds in. |
| NuGet supply chain → Application.csproj | Three new package dependencies enter the codebase here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | PersonNotFoundException message | accept | Message contains only the requested int ID — no PII beyond what the caller already supplied. PersonId property surfaces the same id structurally so Phase 4 can build Problem Details without scraping the message. |
| T-02-02 | Tampering | DTO record definitions (mass assignment) | mitigate | Each DTO is an explicit C# record with named positional parameters — no dynamic property bag, no inheritance, no `[JsonExtensionData]`. Unknown JSON fields will be ignored by System.Text.Json defaults in Phase 4. |
| T-02-03 | Information Disclosure | Nullable repository contract (D-03) | mitigate | `Task<Person?> GetByIdAsync` returns null instead of throwing — handlers (Plan 02) translate null to PersonNotFoundException at the application boundary, ensuring 404 Problem Details in Phase 4 rather than a leaked stack trace. |
| T-02-SC | Tampering | Mediator.Abstractions 3.0.2, FluentValidation 12.1.1, FluentValidation.DependencyInjectionExtensions 12.1.1 (NuGet) | accept | RESEARCH.md Package Legitimacy Audit verified all three packages directly via nuget.org and the official martinothamar/Mediator + FluentValidation/FluentValidation GitHub repos: 7.4M / 932M / 415M downloads, multi-year histories, MIT/Apache 2.0 licenses. slopcheck does not support the NuGet ecosystem (npm only). No `[ASSUMED]` / `[SUS]` / `[SLOP]` packages — no blocking human checkpoint required. |
</threat_model>

<verification>
- `dotnet build PersonsAPI.sln -c Debug` exits 0
- `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` exits 0 with 2 passing DTO tests
- `grep -rE "Microsoft\.(EntityFrameworkCore|AspNetCore)" src/PersonsAPI.Application/` returns no matches (Application has no Infrastructure or ASP.NET Core surface)
- `grep -c "Mediator.SourceGenerator" src/PersonsAPI.Application/PersonsAPI.Application.csproj` returns 0 (Pitfall 1 prevented)
- All artifacts in `must_haves.artifacts` exist
- INFRA-03 is satisfied: IPersonRepository declares in PersonsAPI.Application.Ports namespace
</verification>

<success_criteria>
- The PersonsAPI.Application project compiles standalone against Domain + Mediator.Abstractions + FluentValidation, with zero EF Core / ASP.NET Core / Mediator.SourceGenerator references.
- IPersonRepository is the only port interface; it returns Task (not ValueTask) and uses nullable Person? on the lookup method.
- PersonNotFoundException is sealed, inherits directly from System.Exception, and exposes a PersonId property.
- Four distinct DTO records exist with the exact nullability profile from D-05; PersonResponse.FromDomain is the single static mapping factory (no AutoMapper).
- The xUnit test harness for Application exists, references both Application and Domain, and runs green.
</success_criteria>

<output>
Create `.planning/phases/02-application-layer/02-01-SUMMARY.md` recording: artifacts created, decisions implemented (D-03, D-04, D-05, D-06, D-07), the IPersonRepository signature surface for downstream plans, and the package set installed.
</output>
