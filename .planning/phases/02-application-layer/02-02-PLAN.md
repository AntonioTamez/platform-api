---
phase: 02-application-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs
  - src/PersonsAPI.Application/Queries/GetPersonByIdQuery.cs
  - src/PersonsAPI.Application/Commands/CreatePersonCommand.cs
  - src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs
  - src/PersonsAPI.Application/Commands/PatchPersonCommand.cs
  - src/PersonsAPI.Application/Commands/DeletePersonCommand.cs
  - tests/PersonsAPI.Application.Tests/Commands/CreatePersonCommandValidatorTests.cs
  - tests/PersonsAPI.Application.Tests/Commands/PatchPersonCommandValidatorTests.cs
autonomous: true
requirements: [READ-01, READ-02, WRITE-01, WRITE-02, WRITE-03, WRITE-04]
user_setup: []

must_haves:
  truths:
    - "READ-01: GetAllPersonsQuery + GetAllPersonsHandler returns IReadOnlyList<PersonResponse> mapped from IPersonRepository.GetAllAsync"
    - "READ-02: GetPersonByIdQuery(int Id) + GetPersonByIdHandler throws PersonNotFoundException when GetByIdAsync returns null (D-03/D-04)"
    - "WRITE-01: CreatePersonCommand + CreatePersonHandler calls Person.Create() (never sets properties) then repository.AddAsync, returning PersonResponse"
    - "WRITE-02: UpdatePersonCommand(int Id, UpdatePersonRequest Dto) + UpdatePersonHandler fetches via GetByIdAsync, throws PersonNotFoundException on miss, calls Person.UpdateName + Person.UpdateDateOfBirth, then repository.UpdateAsync"
    - "WRITE-03: PatchPersonCommand(int Id, UpdatePersonDto Dto) + PatchPersonHandler applies only non-null fields with the `dto.Field ?? person.Field` fallback pattern (D-02, Pitfall 6 guard)"
    - "WRITE-04: DeletePersonCommand(int Id) : ICommand<Unit> + DeletePersonHandler fetches then deletes the entity, throwing PersonNotFoundException on miss, returning Unit.Value"
    - "D-08, D-09: Three FluentValidation validators (CreatePersonCommandValidator, UpdatePersonCommandValidator, PatchPersonCommandValidator) exist; queries have no validator"
    - "PATCH validator uses FluentValidation When() conditions so null fields do NOT trigger NotEmpty failures (Pitfall 5 guard)"
    - "All handlers declare `public async ValueTask<TResponse> Handle(...)` — Mediator 3.x signature, NOT MediatR's Task<T> (Pitfall 2)"
  artifacts:
    - path: "src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs"
      provides: "GetAllPersonsQuery : IQuery<IReadOnlyList<PersonResponse>> + GetAllPersonsHandler"
      contains: "IQueryHandler<GetAllPersonsQuery"
    - path: "src/PersonsAPI.Application/Queries/GetPersonByIdQuery.cs"
      provides: "GetPersonByIdQuery(int Id) : IQuery<PersonResponse> + GetPersonByIdHandler"
      contains: "throw new PersonNotFoundException"
    - path: "src/PersonsAPI.Application/Commands/CreatePersonCommand.cs"
      provides: "CreatePersonCommand + CreatePersonCommandValidator + CreatePersonHandler"
      contains: "Person.Create("
    - path: "src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs"
      provides: "UpdatePersonCommand(int Id, UpdatePersonRequest Dto) + UpdatePersonCommandValidator + UpdatePersonHandler"
      contains: "person.UpdateName"
    - path: "src/PersonsAPI.Application/Commands/PatchPersonCommand.cs"
      provides: "PatchPersonCommand(int Id, UpdatePersonDto Dto) + PatchPersonCommandValidator + PatchPersonHandler"
      contains: "dto.FirstName ?? person.FirstName"
    - path: "src/PersonsAPI.Application/Commands/DeletePersonCommand.cs"
      provides: "DeletePersonCommand(int Id) : ICommand<Unit> + DeletePersonHandler"
      contains: "ICommand<Unit>"
    - path: "tests/PersonsAPI.Application.Tests/Commands/CreatePersonCommandValidatorTests.cs"
      provides: "Validator unit tests for CreatePersonCommandValidator (happy path + each failure mode)"
      contains: "CreatePersonCommandValidator"
    - path: "tests/PersonsAPI.Application.Tests/Commands/PatchPersonCommandValidatorTests.cs"
      provides: "Validator unit tests for PatchPersonCommandValidator (null-skip behavior + non-null rules)"
      contains: "PatchPersonCommandValidator"
  key_links:
    - from: "src/PersonsAPI.Application/Commands/CreatePersonCommand.cs"
      to: "PersonsAPI.Domain.Entities.Person.Create"
      via: "static factory call in handler"
      pattern: "Person\\.Create\\("
    - from: "src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs"
      to: "PersonsAPI.Domain.Entities.Person.UpdateName"
      via: "instance method call in handler"
      pattern: "person\\.UpdateName\\("
    - from: "src/PersonsAPI.Application/Commands/PatchPersonCommand.cs"
      to: "PersonsAPI.Application.DTOs.UpdatePersonDto"
      via: "PatchPersonCommand record parameter"
      pattern: "UpdatePersonDto Dto"
    - from: "All six handlers"
      to: "PersonsAPI.Application.Ports.IPersonRepository"
      via: "primary constructor DI"
      pattern: "IPersonRepository repository"
---

<objective>
Implement every CQRS use-case the Application layer owns: two queries (GetAllPersons, GetPersonById), four commands (Create, Update, Patch, Delete), and three FluentValidation validators co-located with their write commands. Each handler uses the Mediator 3.x signature (ValueTask, ICommand/IQuery/ICommandHandler/IQueryHandler) and delegates business logic to the domain entity (Person.Create, Person.UpdateName, Person.UpdateDateOfBirth) — handlers never assign properties directly. This closes READ-01, READ-02, WRITE-01, WRITE-02, WRITE-03, and WRITE-04 at the Application-layer level.

Purpose: Phase 2 success criterion #2 ("each CRUD + PATCH operation has a command or query record plus a handler") and the validator scope decided in D-08/D-09 land here. The validators do nothing yet at runtime — Plan 03 wires them in via ValidationBehavior — but they MUST exist and unit-test correctly so the pipeline behavior has validators to dispatch to.
Output: Six CQRS files under src/PersonsAPI.Application/Queries/ and Commands/, plus two xUnit test files covering the most failure-prone validators (Create — happy path/failure mode coverage, and Patch — the null-skip behavior that the Pitfall 5 guard relies on).
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-application-layer/02-CONTEXT.md
@.planning/phases/02-application-layer/02-RESEARCH.md
@.planning/phases/02-application-layer/02-PATTERNS.md
@src/PersonsAPI.Domain/Entities/Person.cs
@.planning/phases/02-application-layer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both query handlers (READ-01 GetAllPersons, READ-02 GetPersonById)</name>
  <files>src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs, src/PersonsAPI.Application/Queries/GetPersonByIdQuery.cs</files>
  <read_first>
    - src/PersonsAPI.Application/Ports/IPersonRepository.cs (GetAllAsync and GetByIdAsync signatures from Plan 01)
    - src/PersonsAPI.Application/DTOs/PersonResponse.cs (FromDomain static factory)
    - src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs (constructor that takes int id)
    - .planning/phases/02-application-layer/02-PATTERNS.md (Pattern Assignments for GetAllPersonsQuery.cs and GetPersonByIdQuery.cs — canonical templates)
    - .planning/phases/02-application-layer/02-RESEARCH.md (Pattern 1 record syntax with IQuery<T>; Pattern 2 handler with ValueTask<T>; Pitfall 2 — ValueTask not Task)
    - .planning/phases/02-application-layer/02-CONTEXT.md (D-03, D-04 not-found contract; D-08 no validators for queries)
  </read_first>
  <action>
Create two files under `src/PersonsAPI.Application/Queries/`. Each file contains BOTH the query record and its handler (per PATTERNS.md "Command and handler can live in the same file" — consistent with Phase 1 single-file approach). Use Mediator 3.x interfaces from the `Mediator` namespace (the `Mediator.Abstractions` 3.0.2 package).

File 1 — `src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs` (READ-01):
- Namespace `PersonsAPI.Application.Queries`.
- usings: `Mediator;`, `PersonsAPI.Application.DTOs;`, `PersonsAPI.Application.Ports;`.
- `public record GetAllPersonsQuery : IQuery<IReadOnlyList<PersonResponse>>;` — parameterless record; the colon-base syntax is correct for an interface implementation on a parameterless record.
- `public sealed class GetAllPersonsHandler(IPersonRepository repository) : IQueryHandler<GetAllPersonsQuery, IReadOnlyList<PersonResponse>>` — primary constructor (C# 14), sealed.
- `public async ValueTask<IReadOnlyList<PersonResponse>> Handle(GetAllPersonsQuery query, CancellationToken cancellationToken)` — body: `var persons = await repository.GetAllAsync(cancellationToken);` then `return persons.Select(PersonResponse.FromDomain).ToList().AsReadOnly();`. Use `System.Linq.Enumerable.Select` (available via ImplicitUsings).
- Return type is `ValueTask<T>` — NOT `Task<T>` (Pitfall 2).
- XML doc summaries on both the record and the handler.
- No validator class — read queries do not carry user-supplied body data (D-08).

File 2 — `src/PersonsAPI.Application/Queries/GetPersonByIdQuery.cs` (READ-02):
- Namespace `PersonsAPI.Application.Queries`.
- usings: `Mediator;`, `PersonsAPI.Application.DTOs;`, `PersonsAPI.Application.Exceptions;`, `PersonsAPI.Application.Ports;`.
- `public record GetPersonByIdQuery(int Id) : IQuery<PersonResponse>;` — single positional `int Id` parameter.
- `public sealed class GetPersonByIdHandler(IPersonRepository repository) : IQueryHandler<GetPersonByIdQuery, PersonResponse>`.
- `public async ValueTask<PersonResponse> Handle(GetPersonByIdQuery query, CancellationToken cancellationToken)` — body uses the null-coalescing throw one-liner: `var person = await repository.GetByIdAsync(query.Id, cancellationToken) ?? throw new PersonNotFoundException(query.Id);` then `return PersonResponse.FromDomain(person);`. This is the canonical READ-02 not-found contract (D-03 / D-04).
- ValueTask return, sealed class, XML doc summaries on both types.

After authoring both files, run `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` — must succeed.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug 2>&1 | grep -E "Build succeeded|0 Error"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs` contains "public record GetAllPersonsQuery : IQuery<IReadOnlyList<PersonResponse>>" and "public sealed class GetAllPersonsHandler"
    - The handler's Handle method signature contains "async ValueTask<IReadOnlyList<PersonResponse>> Handle" (verified by grep)
    - `grep -c "Task<" src/PersonsAPI.Application/Queries/GetAllPersonsQuery.cs` returns 0 outside `ValueTask` (Pitfall 2 guard — no `Task<T>` return type)
    - The handler body calls `repository.GetAllAsync(cancellationToken)` and uses `PersonResponse.FromDomain` as the projection function
    - `src/PersonsAPI.Application/Queries/GetPersonByIdQuery.cs` contains "public record GetPersonByIdQuery(int Id) : IQuery<PersonResponse>" and "public sealed class GetPersonByIdHandler"
    - The same file contains "?? throw new PersonNotFoundException(query.Id)" (null-coalescing throw — D-04)
    - The same file contains "async ValueTask<PersonResponse> Handle"
    - `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` exits 0
    - No validator class is defined in either query file (D-08)
  </acceptance_criteria>
  <done>Both queries compile. READ-01 returns a read-only list via PersonResponse.FromDomain mapping; READ-02 throws PersonNotFoundException when GetByIdAsync returns null. Both handlers use the Mediator 3.x ValueTask signature.</done>
</task>

<task type="auto">
  <name>Task 2: Author Create, Update, Delete commands with validators and handlers (WRITE-01, WRITE-02, WRITE-04)</name>
  <files>src/PersonsAPI.Application/Commands/CreatePersonCommand.cs, src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs, src/PersonsAPI.Application/Commands/DeletePersonCommand.cs, tests/PersonsAPI.Application.Tests/Commands/CreatePersonCommandValidatorTests.cs</files>
  <read_first>
    - src/PersonsAPI.Domain/Entities/Person.cs (Person.Create signature, Person.UpdateName signature with all three name params, Person.UpdateDateOfBirth signature)
    - src/PersonsAPI.Application/Ports/IPersonRepository.cs (AddAsync, GetByIdAsync, UpdateAsync, DeleteAsync signatures all take Person + CancellationToken)
    - src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs, UpdatePersonRequest.cs (DTO field order: FirstName, PaternalLastName, MaternalLastName, DateOfBirth)
    - src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs (constructor that takes int id)
    - .planning/phases/02-application-layer/02-PATTERNS.md (Pattern Assignments for CreatePersonCommand.cs, UpdatePersonCommand.cs, DeletePersonCommand.cs — canonical command+validator+handler triples)
    - .planning/phases/02-application-layer/02-RESEARCH.md (Pattern 6 validator template with NotEmpty + MinimumLength 2 + MaximumLength 100 + DateOfBirth Must(d => d <= today); Pitfall 2 ValueTask; Pitfall 6 — UpdateName requires all three name params)
    - .planning/phases/02-application-layer/02-CONTEXT.md (D-08 validator scope; D-09 validators mirror domain invariants intentionally)
    - tests/PersonsAPI.Domain.Tests/PersonTests.cs (xUnit style baseline for fact/theory patterns)
  </read_first>
  <action>
Create three command files and one validator unit-test file. Each command file holds the command record, its validator (for Create and Update; Delete has no validator), and its handler — one self-contained unit per use case.

File 1 — `src/PersonsAPI.Application/Commands/CreatePersonCommand.cs` (WRITE-01):
- Namespace `PersonsAPI.Application.Commands`.
- usings: `FluentValidation;`, `Mediator;`, `PersonsAPI.Application.DTOs;`, `PersonsAPI.Application.Ports;`, `PersonsAPI.Domain.Entities;`.
- `public record CreatePersonCommand(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth) : ICommand<PersonResponse>;` — four positional params in the order matching CreatePersonRequest.
- `public sealed class CreatePersonCommandValidator : AbstractValidator<CreatePersonCommand>` — constructor declares four RuleFor chains exactly as in RESEARCH.md Pattern 6: FirstName / PaternalLastName / MaternalLastName each `.NotEmpty().MinimumLength(2).MaximumLength(100)`; DateOfBirth `.Must(d => d <= DateOnly.FromDateTime(DateTime.Today)).WithMessage("DateOfBirth cannot be in the future.")`. These rules mirror Person.cs's `ValidateName` and `ValidateDateOfBirth` invariants (D-09 — intentional duplication is correct layering).
- `public sealed class CreatePersonHandler(IPersonRepository repository) : ICommandHandler<CreatePersonCommand, PersonResponse>` — primary constructor.
- `public async ValueTask<PersonResponse> Handle(CreatePersonCommand command, CancellationToken cancellationToken)` — body builds `var person = Person.Create(command.FirstName, command.PaternalLastName, command.MaternalLastName, command.DateOfBirth);` then `await repository.AddAsync(person, cancellationToken);` then `return PersonResponse.FromDomain(person);`. The handler MUST call `Person.Create()` — never set Person properties directly (Person has private setters; setter access is impossible — verified in src/PersonsAPI.Domain/Entities/Person.cs).

File 2 — `src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs` (WRITE-02, PUT — full replacement):
- Namespace `PersonsAPI.Application.Commands`.
- usings add `PersonsAPI.Application.Exceptions;`.
- `public record UpdatePersonCommand(int Id, UpdatePersonRequest Dto) : ICommand<PersonResponse>;` — two positional params per RESEARCH.md (the int Id is the route parameter Phase 4 will supply; UpdatePersonRequest is the body).
- `public sealed class UpdatePersonCommandValidator : AbstractValidator<UpdatePersonCommand>` — RuleFor chains target `x => x.Dto.FirstName`, `x => x.Dto.PaternalLastName`, `x => x.Dto.MaternalLastName` each with `.NotEmpty().MinimumLength(2).MaximumLength(100)`; `x => x.Dto.DateOfBirth` with the same `Must(d => d <= today)` rule and "DateOfBirth cannot be in the future." message (D-09).
- `public sealed class UpdatePersonHandler(IPersonRepository repository) : ICommandHandler<UpdatePersonCommand, PersonResponse>`.
- Handler body: `var person = await repository.GetByIdAsync(command.Id, cancellationToken) ?? throw new PersonNotFoundException(command.Id);` then `person.UpdateName(command.Dto.FirstName, command.Dto.PaternalLastName, command.Dto.MaternalLastName);` then `person.UpdateDateOfBirth(command.Dto.DateOfBirth);` then `await repository.UpdateAsync(person, cancellationToken);` then `return PersonResponse.FromDomain(person);`. Two separate domain calls (UpdateName + UpdateDateOfBirth) — do NOT combine.

File 3 — `src/PersonsAPI.Application/Commands/DeletePersonCommand.cs` (WRITE-04):
- Namespace `PersonsAPI.Application.Commands`.
- usings: `Mediator;`, `PersonsAPI.Application.Exceptions;`, `PersonsAPI.Application.Ports;` (no FluentValidation — Delete has no validator per D-08 since the only input is the route id).
- `public record DeletePersonCommand(int Id) : ICommand<Unit>;` — uses Mediator's `Unit` type (not MediatR.Unit).
- `public sealed class DeletePersonHandler(IPersonRepository repository) : ICommandHandler<DeletePersonCommand, Unit>`.
- Handler body: `var person = await repository.GetByIdAsync(command.Id, cancellationToken) ?? throw new PersonNotFoundException(command.Id);` then `await repository.DeleteAsync(person, cancellationToken);` then `return Unit.Value;`. Pass the Person entity (not the int id) to `DeleteAsync` — EF Core tracked-entity pattern (Plan 01 IPersonRepository signature).

File 4 — `tests/PersonsAPI.Application.Tests/Commands/CreatePersonCommandValidatorTests.cs`:
- Namespace `PersonsAPI.Application.Tests.Commands`.
- usings: `PersonsAPI.Application.Commands;`, `FluentValidation.TestHelper;` (FluentValidation 12.1.1 ships TestHelper with `ShouldNotHaveAnyValidationErrors()` and `ShouldHaveValidationErrorFor()`).
- Tests using `[Fact]` and `[Theory]`/`[InlineData]`:
  - `Validate_HappyPath_HasNoErrors`: build CreatePersonCommand with valid values ("Ada", "Lovelace", "Byron", DateOnly today.AddYears(-30)); call `validator.TestValidate(command).ShouldNotHaveAnyValidationErrors();`.
  - `[Theory] [InlineData("")] [InlineData(" ")] [InlineData("A")] Validate_FirstNameEmptyOrTooShort_HasError(string firstName)`: assert `ShouldHaveValidationErrorFor(c => c.FirstName)`.
  - `Validate_FirstNameTooLong_HasError`: pass a 101-character string; assert `ShouldHaveValidationErrorFor(c => c.FirstName)`.
  - `Validate_DateOfBirthInFuture_HasError`: pass `DateOnly.FromDateTime(DateTime.Today).AddDays(1)`; assert `ShouldHaveValidationErrorFor(c => c.DateOfBirth)` with `.WithErrorMessage("DateOfBirth cannot be in the future.")`.

After all four files, run `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` — all tests (including the DTO tests from Plan 01) must pass.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj --no-restore 2>&1 | grep -E "Passed!|Passed:.*Failed: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/Commands/CreatePersonCommand.cs` contains the record line "public record CreatePersonCommand(string FirstName, string PaternalLastName, string MaternalLastName, DateOnly DateOfBirth) : ICommand<PersonResponse>"
    - That file contains "public sealed class CreatePersonCommandValidator : AbstractValidator<CreatePersonCommand>" and "public sealed class CreatePersonHandler"
    - The handler body contains "Person.Create(" (the static factory call) and "repository.AddAsync"
    - `grep -c "command.FirstName = " src/PersonsAPI.Application/Commands/CreatePersonCommand.cs` returns 0 (no direct property assignment on Person)
    - `src/PersonsAPI.Application/Commands/UpdatePersonCommand.cs` contains "public record UpdatePersonCommand(int Id, UpdatePersonRequest Dto)" and "person.UpdateName(command.Dto.FirstName, command.Dto.PaternalLastName, command.Dto.MaternalLastName)" and "person.UpdateDateOfBirth(command.Dto.DateOfBirth)"
    - That file contains the not-found pattern "?? throw new PersonNotFoundException(command.Id)"
    - `src/PersonsAPI.Application/Commands/DeletePersonCommand.cs` contains "public record DeletePersonCommand(int Id) : ICommand<Unit>" and "return Unit.Value"
    - `grep -c "FluentValidation" src/PersonsAPI.Application/Commands/DeletePersonCommand.cs` returns 0 (Delete has no validator)
    - All three handlers use "async ValueTask<" signatures — `grep -rE "async Task<" src/PersonsAPI.Application/Commands/` returns 0 (Pitfall 2)
    - `dotnet test` reports the new CreatePersonCommandValidatorTests passing (at least 6 tests total including theory cases)
    - `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` exits 0
  </acceptance_criteria>
  <done>Three write commands compile with their validators (where applicable) and handlers. WRITE-01 calls Person.Create; WRITE-02 calls Person.UpdateName + Person.UpdateDateOfBirth and throws PersonNotFoundException on miss; WRITE-04 fetches then deletes and returns Unit.Value. Validator tests prove field-level error messages match RESEARCH.md Pattern 6.</done>
</task>

<task type="auto">
  <name>Task 3: Author PatchPersonCommand with conditional validator and null-fallback handler (WRITE-03, D-02, Pitfall 5+6 guards)</name>
  <files>src/PersonsAPI.Application/Commands/PatchPersonCommand.cs, tests/PersonsAPI.Application.Tests/Commands/PatchPersonCommandValidatorTests.cs</files>
  <read_first>
    - src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs (nullable field record: string?, string?, string?, DateOnly?)
    - src/PersonsAPI.Application/Ports/IPersonRepository.cs (GetByIdAsync returns Person?; UpdateAsync takes Person)
    - src/PersonsAPI.Application/Exceptions/PersonNotFoundException.cs
    - src/PersonsAPI.Domain/Entities/Person.cs (UpdateName requires THREE non-null names — calling with null throws DomainException; UpdateDateOfBirth requires non-null DateOnly)
    - .planning/phases/02-application-layer/02-PATTERNS.md (Pattern Assignment for PatchPersonCommand.cs — full canonical command/validator/handler with When() conditions and `dto.Field ?? person.Field` fallback)
    - .planning/phases/02-application-layer/02-RESEARCH.md (Pattern 8 PatchPersonHandler implementation; Pitfall 5 — UpdatePersonDto vs UpdatePersonRequest must not be conflated; Pitfall 6 — UpdateName must never receive null)
    - .planning/phases/02-application-layer/02-CONTEXT.md (D-02 nullable-field application semantics; D-08/D-09 validator scope)
  </read_first>
  <action>
This task is the highest-risk file in the phase (RESEARCH.md flags Pitfalls 5 and 6 here). Follow PATTERNS.md Pattern Assignment for `PatchPersonCommand.cs` exactly — the When() conditions and the null-fallback pattern are non-negotiable.

File 1 — `src/PersonsAPI.Application/Commands/PatchPersonCommand.cs` (WRITE-03):
- Namespace `PersonsAPI.Application.Commands`.
- usings: `FluentValidation;`, `Mediator;`, `PersonsAPI.Application.DTOs;`, `PersonsAPI.Application.Exceptions;`, `PersonsAPI.Application.Ports;`.
- `public record PatchPersonCommand(int Id, UpdatePersonDto Dto) : ICommand<PersonResponse>;` — the Dto parameter type MUST be UpdatePersonDto (the nullable-field record), NOT UpdatePersonRequest (Pitfall 5).
- `public sealed class PatchPersonCommandValidator : AbstractValidator<PatchPersonCommand>` — constructor uses FluentValidation's `When(predicate, ruleBlock)` to scope rules so null fields skip the rule entirely:
  - `When(x => x.Dto.FirstName is not null, () => { RuleFor(x => x.Dto.FirstName!).NotEmpty().MinimumLength(2).MaximumLength(100); });`
  - Same When() block for PaternalLastName and MaternalLastName.
  - `When(x => x.Dto.DateOfBirth is not null, () => { RuleFor(x => x.Dto.DateOfBirth!.Value).Must(d => d <= DateOnly.FromDateTime(DateTime.Today)).WithMessage("DateOfBirth cannot be in the future."); });`
  - The `!` null-forgiving operator inside the When() block is intentional — the predicate proves the value is non-null. Without When(), `.NotEmpty()` on a null string would fire (Pitfall 5).

- `public sealed class PatchPersonHandler(IPersonRepository repository) : ICommandHandler<PatchPersonCommand, PersonResponse>`.
- Handler body (verbatim semantics from RESEARCH.md Pattern 8 and PATTERNS.md Pattern Assignment):
  - Fetch + null check: `var person = await repository.GetByIdAsync(command.Id, cancellationToken) ?? throw new PersonNotFoundException(command.Id);`
  - Local alias: `var dto = command.Dto;`
  - Conditional name update — guard with explicit OR so UpdateName is called only when at least one name field is non-null (Pitfall 6):
    - `if (dto.FirstName is not null || dto.PaternalLastName is not null || dto.MaternalLastName is not null) person.UpdateName(dto.FirstName ?? person.FirstName, dto.PaternalLastName ?? person.PaternalLastName, dto.MaternalLastName ?? person.MaternalLastName);`
    - The `dto.X ?? person.X` fallback ensures Person.UpdateName receives non-null strings — calling UpdateName with null throws DomainException ("X cannot be null...") (verified in src/PersonsAPI.Domain/Entities/Person.cs line 130-134).
  - Conditional date update: `if (dto.DateOfBirth is not null) person.UpdateDateOfBirth(dto.DateOfBirth.Value);` — note `.Value` to unwrap the nullable DateOnly.
  - Persist + map: `await repository.UpdateAsync(person, cancellationToken);` then `return PersonResponse.FromDomain(person);`.

File 2 — `tests/PersonsAPI.Application.Tests/Commands/PatchPersonCommandValidatorTests.cs`:
- Namespace `PersonsAPI.Application.Tests.Commands`.
- usings: `PersonsAPI.Application.Commands;`, `PersonsAPI.Application.DTOs;`, `FluentValidation.TestHelper;`.
- Tests that prove the When() conditions work correctly:
  - `Validate_AllFieldsNull_HasNoErrors`: build `PatchPersonCommand(1, new UpdatePersonDto(null, null, null, null))`, call `validator.TestValidate(command).ShouldNotHaveAnyValidationErrors();` — proves null fields skip validation (Pitfall 5 guard).
  - `Validate_OnlyFirstNameProvidedValid_HasNoErrors`: dto with only `FirstName = "Ada"`, others null — no errors.
  - `Validate_FirstNameProvidedEmpty_HasError`: dto with `FirstName = ""`, others null — `ShouldHaveValidationErrorFor(c => c.Dto.FirstName)`.
  - `Validate_DateOfBirthInFuture_HasError`: dto with `DateOfBirth = today.AddDays(1)`, others null — `ShouldHaveValidationErrorFor(c => c.Dto.DateOfBirth!.Value)` with `.WithErrorMessage("DateOfBirth cannot be in the future.")`.

After both files, run `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj`. All tests from Plans 01 + the Create validator tests + the Patch validator tests must pass (failure count zero).
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform; dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj --no-restore 2>&1 | grep -E "Passed!|Passed:.*Failed: 0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/PersonsAPI.Application/Commands/PatchPersonCommand.cs` contains "public record PatchPersonCommand(int Id, UpdatePersonDto Dto) : ICommand<PersonResponse>"
    - That file contains "public sealed class PatchPersonCommandValidator : AbstractValidator<PatchPersonCommand>"
    - `grep -c "When(x =>" src/PersonsAPI.Application/Commands/PatchPersonCommand.cs` returns >= 4 (one When per nullable field — Pitfall 5 guard)
    - That file contains "dto.FirstName ?? person.FirstName" and "dto.PaternalLastName ?? person.PaternalLastName" and "dto.MaternalLastName ?? person.MaternalLastName" (null-fallback pattern — Pitfall 6 guard)
    - That file contains "if (dto.FirstName is not null || dto.PaternalLastName is not null || dto.MaternalLastName is not null)" (no-op-update guard for name)
    - That file contains "if (dto.DateOfBirth is not null) person.UpdateDateOfBirth(dto.DateOfBirth.Value)"
    - That file contains "?? throw new PersonNotFoundException(command.Id)" and "PersonResponse.FromDomain(person)"
    - The handler signature uses "async ValueTask<PersonResponse> Handle" (Pitfall 2)
    - `grep -c "UpdatePersonRequest" src/PersonsAPI.Application/Commands/PatchPersonCommand.cs` returns 0 (Pitfall 5 — UpdatePersonDto only, never UpdatePersonRequest)
    - `tests/PersonsAPI.Application.Tests/Commands/PatchPersonCommandValidatorTests.cs` contains all four named tests (Validate_AllFieldsNull_HasNoErrors, Validate_OnlyFirstNameProvidedValid_HasNoErrors, Validate_FirstNameProvidedEmpty_HasError, Validate_DateOfBirthInFuture_HasError)
    - `dotnet test` exits 0 with all DTO + CreatePersonCommandValidator + PatchPersonCommandValidator tests passing
    - `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` exits 0
  </acceptance_criteria>
  <done>PatchPersonCommand, its conditional validator using When(), and its null-fallback handler all compile. The validator skips rules when fields are null; the handler never passes null to Person.UpdateName/UpdateDateOfBirth. Both Pitfall 5 and Pitfall 6 are structurally prevented by the source. WRITE-03 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → CQRS command/query record | All write inputs cross from Phase 4 controller into Application via Mediator.Send(command). Tampering / out-of-range data enters here. |
| CQRS handler → Domain entity method | Person.Create / UpdateName / UpdateDateOfBirth are the only mutation entry points — handlers may not bypass them. |
| Handler → IPersonRepository (port) | Application's only persistence surface. Whatever the handler passes here is whatever Phase 3 will persist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | CreatePersonCommand / UpdatePersonCommand input fields | mitigate | CreatePersonCommandValidator + UpdatePersonCommandValidator each enforce NotEmpty + length 2–100 on all three name fields and `DateOfBirth <= today` (D-09). Domain Person.Create / UpdateName / UpdateDateOfBirth re-validate as a second line of defense (VAL-02 contract from Phase 1). |
| T-02-05 | Tampering | PatchPersonCommand — partial update bypassing validation | mitigate | PatchPersonCommandValidator uses FluentValidation `When(field is not null, ...)` blocks so provided fields are still validated to the same bounds, while null fields are skipped intentionally. Pitfall 5 guard. |
| T-02-06 | Tampering | PatchPersonHandler — null reaching Person.UpdateName | mitigate | Handler uses `dto.Field ?? person.Field` fallback before calling UpdateName, and only calls UpdateName when at least one name field is non-null. Pitfall 6 guard. Domain's `ValidateName` is the second line of defense; even if a regression nulls a field, Person.UpdateName throws DomainException. |
| T-02-07 | Elevation of Privilege | Direct property assignment on Person | mitigate | Person properties are all `private set` (verified in src/PersonsAPI.Domain/Entities/Person.cs). Handlers MUST call Person.Create / UpdateName / UpdateDateOfBirth. Static-property assignment would not compile — the type system enforces this. |
| T-02-08 | Information Disclosure | Validation error messages | accept | FluentValidation default messages name the field that failed but do not include the rejected value beyond what the caller supplied. No PII risk in this layer; Phase 4 will wrap the messages in Problem Details. |
| T-02-SC | Tampering | NuGet supply chain | accept | Same disposition as Plan 02-01 — Mediator.Abstractions and FluentValidation packages were verified in Plan 01. No new packages added in this plan. |
</threat_model>

<verification>
- `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj -c Debug` exits 0
- `dotnet test tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` exits 0 with all DTO + CreatePersonCommandValidator + PatchPersonCommandValidator tests passing
- `grep -rE "async Task<" src/PersonsAPI.Application/Commands/ src/PersonsAPI.Application/Queries/` returns 0 (Pitfall 2 — every handler uses ValueTask)
- `grep -rE "RequestHandlerDelegate" src/PersonsAPI.Application/` returns 0 (Pitfall 3 — MediatR naming forbidden)
- `grep -rE "IRequest<" src/PersonsAPI.Application/` returns 0 (Mediator 3.x prefers ICommand/IQuery — RESEARCH.md State of the Art)
- Six CQRS files (two queries + four commands) all under `src/PersonsAPI.Application/Queries/` or `src/PersonsAPI.Application/Commands/`
- Three validator classes exist: CreatePersonCommandValidator, UpdatePersonCommandValidator, PatchPersonCommandValidator
- No validator class exists for either query or for DeletePersonCommand (D-08)
</verification>

<success_criteria>
- READ-01: GetAllPersonsQuery + handler compiled, mapping via PersonResponse.FromDomain
- READ-02: GetPersonByIdQuery + handler throws PersonNotFoundException on null
- WRITE-01: CreatePersonCommand + validator + handler; handler calls Person.Create
- WRITE-02: UpdatePersonCommand + validator + handler; handler calls Person.UpdateName + Person.UpdateDateOfBirth
- WRITE-03: PatchPersonCommand with When()-conditional validator and null-fallback handler — Pitfalls 5 and 6 structurally guarded
- WRITE-04: DeletePersonCommand + handler returning Unit.Value
- All six handlers use ValueTask<T> (Mediator 3.x), primary constructors (C# 14), sealed concrete classes (PATTERNS.md Shared Patterns)
- Validator unit tests cover happy-path + each failure mode for CreatePersonCommandValidator and the null-skip semantics for PatchPersonCommandValidator
</success_criteria>

<output>
Create `.planning/phases/02-application-layer/02-02-SUMMARY.md` recording: every CQRS type and handler created (with file path and the requirement ID it closes), the validator behavior decisions (D-08, D-09), the Pitfall 5 + Pitfall 6 guards implemented in PatchPersonHandler, and any deviations from PATTERNS.md (there should be none).
</output>
