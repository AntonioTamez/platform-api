---
phase: 04-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs
  - src/PersonsAPI.Api/PersonsAPI.Api.csproj
  - src/PersonsAPI.Api/Program.cs
  - src/PersonsAPI.Api/Properties/launchSettings.json
  - src/PersonsAPI.Api/appsettings.json
  - src/PersonsAPI.Api/appsettings.Development.json
  - PersonsAPI.sln
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - ERR-01
user_setup: []

must_haves:
  truths:
    - "UpdatePersonDto is a mutable class with { get; set; } properties so JsonPatchDocument<T>.ApplyTo() can write to it without throwing"
    - "PersonsAPI.Api project compiles, references Application + Infrastructure, and is the sole project that installs Mediator.SourceGenerator"
    - "Program.cs is the sole composition root and registers Controllers, ProblemDetails, exception handlers, OpenApi, Mediator with ValidationBehavior pipeline, Application validators, and Infrastructure persistence in the documented order"
    - "Middleware pipeline activates UseExceptionHandler (no route argument) BEFORE MapControllers, and MapScalarApiReference is mapped AFTER MapControllers"
    - "DataSeeder.SeedAsync() is awaited on app.Services BEFORE app.RunAsync() so the InMemory store is populated before requests arrive"
    - "Solution file PersonsAPI.sln contains the PersonsAPI.Api project nested under the src solution folder"
  artifacts:
    - path: "src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs"
      provides: "Mutable PATCH target DTO (per D-06/D-08 + RESEARCH Critical Fix)"
      contains: "public class UpdatePersonDto"
    - path: "src/PersonsAPI.Api/PersonsAPI.Api.csproj"
      provides: "Api project with Microsoft.NET.Sdk.Web SDK and required packages"
      contains: "Mediator.SourceGenerator"
    - path: "src/PersonsAPI.Api/Program.cs"
      provides: "Composition root wiring all three layers + Scalar + seeder"
      contains: "AddMediator"
    - path: "PersonsAPI.sln"
      provides: "Solution registration for the new Api project"
      contains: "PersonsAPI.Api"
  key_links:
    - from: "src/PersonsAPI.Api/Program.cs"
      to: "src/PersonsAPI.Application/ServiceCollectionExtensions.cs"
      via: "AddApplication() call after AddMediator()"
      pattern: "AddApplication\\(\\)"
    - from: "src/PersonsAPI.Api/Program.cs"
      to: "src/PersonsAPI.Infrastructure/ServiceCollectionExtensions.cs"
      via: "AddInfrastructure() call after AddApplication()"
      pattern: "AddInfrastructure\\(\\)"
    - from: "src/PersonsAPI.Api/Program.cs"
      to: "src/PersonsAPI.Infrastructure/Seeder/DataSeeder.cs"
      via: "await app.Services.SeedAsync() before app.RunAsync()"
      pattern: "SeedAsync\\(\\)"
---

<objective>
Establish the PersonsAPI.Api project as the sole composition root for Phase 4, fix the JsonPatch incompatibility in UpdatePersonDto, and wire the full ASP.NET Core middleware + DI pipeline so all three layers (Application, Infrastructure, Domain via transitive reference) are connected and ready to host the controller (Plan 02) and integration tests (Plan 03).

Purpose: Without a compilable Api project and a correct composition root, no controller actions can be exercised. UpdatePersonDto must become mutable BEFORE the PATCH endpoint in Plan 02 can apply patches without runtime failure (per D-06/D-08 and RESEARCH Critical Fix). This plan implements ERR-01 (Problem Details registration), DOC-01 (OpenAPI generation), and DOC-02 (Scalar UI mapping) at the wiring level — handlers and controller in Plan 02 complete ERR-02/ERR-03.

Output: A solution that builds successfully with `dotnet build PersonsAPI.sln`, including the new Api project, a mutable UpdatePersonDto, and a Program.cs that calls every required registration and middleware extension in the correct order.
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api-layer/04-CONTEXT.md
@.planning/phases/04-api-layer/04-RESEARCH.md
@.planning/phases/04-api-layer/04-PATTERNS.md
@CLAUDE.md
@src/PersonsAPI.Application/ServiceCollectionExtensions.cs
@src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs
@src/PersonsAPI.Application/Commands/PatchPersonCommand.cs
@src/PersonsAPI.Application/Behaviors/ValidationBehavior.cs
@src/PersonsAPI.Infrastructure/ServiceCollectionExtensions.cs
@src/PersonsAPI.Infrastructure/Seeder/DataSeeder.cs
@src/PersonsAPI.Infrastructure/PersonsAPI.Infrastructure.csproj
@PersonsAPI.sln
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert UpdatePersonDto from positional record to mutable class</name>
  <files>src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs</files>
  <read_first>
    - src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs (current positional record form)
    - src/PersonsAPI.Application/Commands/PatchPersonCommand.cs (consumer of UpdatePersonDto — verify property access pattern survives change)
    - .planning/phases/04-api-layer/04-RESEARCH.md (## Critical Fix: UpdatePersonDto Must Be Mutable)
    - .planning/phases/04-api-layer/04-CONTEXT.md (D-06, D-08)
    - .planning/phases/04-api-layer/04-PATTERNS.md (## UpdatePersonDto.cs — MODIFY section)
  </read_first>
  <behavior>
    - UpdatePersonDto can be instantiated parameterless: new UpdatePersonDto() compiles and produces all-null properties.
    - Each of the four properties is writable after construction: dto.FirstName = "x" assigns successfully.
    - PatchPersonCommandValidator and PatchPersonHandler continue to read dto.FirstName / dto.PaternalLastName / dto.MaternalLastName / dto.DateOfBirth without code changes.
    - The Application project still compiles after the type change.
  </behavior>
  <action>
    Replace the positional record declaration in src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs with a mutable class. The new type MUST: keep namespace PersonsAPI.Application.DTOs; be declared as `public class UpdatePersonDto`; expose exactly four properties — `public string? FirstName { get; set; }`, `public string? PaternalLastName { get; set; }`, `public string? MaternalLastName { get; set; }`, `public DateOnly? DateOfBirth { get; set; }`; have no explicit constructor (default parameterless constructor only). Preserve a `<summary>` XML doc comment that names this as the PATCH target DTO and references decision D-06/D-08 plus the JsonPatchDocument mutability requirement from RESEARCH.md Critical Fix. Do NOT modify PatchPersonCommand.cs, PatchPersonCommandValidator, PatchPersonHandler, or any other file — verify post-change that the Application project still compiles by running dotnet build.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform &amp;&amp; dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj /clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs contains the exact substring `public class UpdatePersonDto`.
    - Source assertion: src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs contains four lines matching the pattern `public (string\?|DateOnly\?) (FirstName|PaternalLastName|MaternalLastName|DateOfBirth) { get; set; }`.
    - Source assertion: src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs does NOT contain the substring `public record UpdatePersonDto(`.
    - Behavior assertion: `dotnet build src/PersonsAPI.Application/PersonsAPI.Application.csproj` exits with code 0 and reports 0 errors.
    - Behavior assertion: `dotnet build tests/PersonsAPI.Application.Tests/PersonsAPI.Application.Tests.csproj` exits with code 0 (consumers of UpdatePersonDto still compile).
  </acceptance_criteria>
  <done>UpdatePersonDto is a mutable class with four nullable get/set properties, the Application project and its tests still compile, no downstream files were modified, and the XML doc comment cites D-06/D-08 plus the JsonPatch mutability rationale.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create PersonsAPI.Api project, install packages, register in solution</name>
  <files>src/PersonsAPI.Api/PersonsAPI.Api.csproj, src/PersonsAPI.Api/Properties/launchSettings.json, src/PersonsAPI.Api/appsettings.json, src/PersonsAPI.Api/appsettings.Development.json, PersonsAPI.sln</files>
  <read_first>
    - src/PersonsAPI.Infrastructure/PersonsAPI.Infrastructure.csproj (csproj structure analog — same PropertyGroup pattern, SDK differs)
    - PersonsAPI.sln (current solution file — note the existing src solution folder GUID {827E0CD3-B72D-47B6-A68D-7590B98EB39B} and how Infrastructure is nested)
    - .planning/phases/04-api-layer/04-RESEARCH.md (## Standard Stack — exact package versions; Pitfall 4 on SourceGenerator scope)
    - .planning/phases/04-api-layer/04-PATTERNS.md (## PersonsAPI.Api.csproj section — full csproj template)
    - CLAUDE.md (Recommended Stack — pinned versions)
  </read_first>
  <behavior>
    - Directory src/PersonsAPI.Api/ exists with a csproj using the Microsoft.NET.Sdk.Web SDK.
    - The Api project references the Application and Infrastructure projects (Domain is transitive).
    - Mediator.SourceGenerator 3.0.2 is installed ONLY in this project (per Pitfall 4 — Application has only Mediator.Abstractions).
    - The four web packages are installed at the exact pinned versions from CLAUDE.md / RESEARCH.md.
    - PersonsAPI.sln contains the PersonsAPI.Api project nested under the existing src solution folder.
    - `dotnet restore PersonsAPI.sln` completes without errors and `dotnet build PersonsAPI.sln` compiles every project (even though Program.cs has not been authored yet — Task 3 creates it).
  </behavior>
  <action>
    Create directory `src/PersonsAPI.Api/`. Create `src/PersonsAPI.Api/PersonsAPI.Api.csproj` with `<Project Sdk="Microsoft.NET.Sdk.Web">`, a PropertyGroup containing `<TargetFramework>net10.0</TargetFramework>`, `<Nullable>enable</Nullable>`, `<ImplicitUsings>enable</ImplicitUsings>`, `<LangVersion>14</LangVersion>`, and `<RootNamespace>PersonsAPI.Api</RootNamespace>`. Add a `<ItemGroup>` with two `<ProjectReference>` entries: `..\PersonsAPI.Application\PersonsAPI.Application.csproj` and `..\PersonsAPI.Infrastructure\PersonsAPI.Infrastructure.csproj`. Add a second `<ItemGroup>` with four `<PackageReference>` entries at exact versions: `Mediator.SourceGenerator` Version `3.0.2` (with `OutputItemType="Analyzer"` and `ReferenceOutputAssembly="false"` per source-generator convention if needed for clean build; if the generator runs cleanly without these attributes use the plain form), `Microsoft.AspNetCore.JsonPatch.SystemTextJson` Version `10.0.8`, `Microsoft.AspNetCore.OpenApi` Version `10.0.8`, `Scalar.AspNetCore` Version `2.14.14`. Do NOT add `Mediator.Abstractions` (transitive via the Application reference). Do NOT install `Newtonsoft`-based JsonPatch packages (per Pitfall — Newtonsoft variant is forbidden). Create a minimal placeholder `src/PersonsAPI.Api/Program.cs` containing a single statement `// Plan 04-01 Task 3 wires services and middleware here.` so the project compiles in this task — Task 3 replaces it. Create `src/PersonsAPI.Api/Properties/launchSettings.json` with two profiles (`http` on port 5000, `https` on ports 5001/5000) and `launchBrowser: true` targeting the relative URL `scalar/v1` so /scalar (Scalar default route) opens on F5. Create `src/PersonsAPI.Api/appsettings.json` containing default `Logging` (LogLevel Default=Information, Microsoft.AspNetCore=Warning) and `AllowedHosts: "*"`. Create `src/PersonsAPI.Api/appsettings.Development.json` containing `Logging` overrides (LogLevel Default=Information, Microsoft.AspNetCore=Warning, Microsoft.EntityFrameworkCore=Information). Register the project in the solution with `dotnet sln C:/ATS/Git/platform/PersonsAPI.sln add C:/ATS/Git/platform/src/PersonsAPI.Api/PersonsAPI.Api.csproj --solution-folder src`. After this, `dotnet restore PersonsAPI.sln` and `dotnet build PersonsAPI.sln` MUST both succeed.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform &amp;&amp; dotnet restore PersonsAPI.sln &amp;&amp; dotnet build PersonsAPI.sln /clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj exists and contains `Sdk="Microsoft.NET.Sdk.Web"`.
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains `<TargetFramework>net10.0</TargetFramework>` AND `<LangVersion>14</LangVersion>`.
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains both `..\PersonsAPI.Application\PersonsAPI.Application.csproj` and `..\PersonsAPI.Infrastructure\PersonsAPI.Infrastructure.csproj` as ProjectReference entries.
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains a PackageReference to `Mediator.SourceGenerator` with Version="3.0.2".
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains a PackageReference to `Microsoft.AspNetCore.JsonPatch.SystemTextJson` with Version="10.0.8".
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains a PackageReference to `Microsoft.AspNetCore.OpenApi` with Version="10.0.8".
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj contains a PackageReference to `Scalar.AspNetCore` with Version="2.14.14".
    - Source assertion: src/PersonsAPI.Api/PersonsAPI.Api.csproj does NOT reference `Microsoft.AspNetCore.JsonPatch` without the `.SystemTextJson` suffix.
    - Source assertion: src/PersonsAPI.Application/PersonsAPI.Application.csproj does NOT add a new reference to `Mediator.SourceGenerator` (Pitfall 4 — generator stays in Api only).
    - Source assertion: PersonsAPI.sln contains the string `PersonsAPI.Api` and a project entry referencing `src\PersonsAPI.Api\PersonsAPI.Api.csproj`.
    - Source assertion: PersonsAPI.sln's `NestedProjects` section nests the Api project GUID under the src folder GUID `{827E0CD3-B72D-47B6-A68D-7590B98EB39B}`.
    - Source assertion: src/PersonsAPI.Api/Properties/launchSettings.json contains `"launchUrl": "scalar/v1"` (or `"launchUrl": "scalar"`) in at least one profile.
    - Source assertion: src/PersonsAPI.Api/appsettings.json and src/PersonsAPI.Api/appsettings.Development.json both exist and are valid JSON.
    - Behavior assertion: `dotnet restore C:/ATS/Git/platform/PersonsAPI.sln` exits with code 0.
    - Behavior assertion: `dotnet build C:/ATS/Git/platform/PersonsAPI.sln` exits with code 0 and reports 0 errors.
  </acceptance_criteria>
  <done>The PersonsAPI.Api project exists with Microsoft.NET.Sdk.Web, the four pinned package versions, project references to Application and Infrastructure, a placeholder Program.cs that compiles, launch/appsettings files in place, and is registered in PersonsAPI.sln under the src solution folder; full-solution restore and build both succeed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Program.cs composition root with DI, middleware, OpenAPI/Scalar, and seeder</name>
  <files>src/PersonsAPI.Api/Program.cs</files>
  <read_first>
    - src/PersonsAPI.Api/Program.cs (placeholder created in Task 2 — replace entirely)
    - src/PersonsAPI.Application/ServiceCollectionExtensions.cs (AddApplication contract + the XML comment documenting Phase 4 responsibilities: AddMediator with ValidationBehavior pipeline + exception handler registration)
    - src/PersonsAPI.Application/Behaviors/ValidationBehavior.cs (open generic name to pass to options.PipelineBehaviors)
    - src/PersonsAPI.Infrastructure/ServiceCollectionExtensions.cs (AddInfrastructure contract)
    - src/PersonsAPI.Infrastructure/Seeder/DataSeeder.cs (SeedAsync extension on IServiceProvider — namespace PersonsAPI.Infrastructure.Seeder; called BEFORE app.RunAsync())
    - .planning/phases/04-api-layer/04-CONTEXT.md (D-01 ordering: NotFound first, Validation second; D-02 AddProblemDetails)
    - .planning/phases/04-api-layer/04-RESEARCH.md (Pattern 3 Program.cs full layout; Pitfall 2 UseExceptionHandler with no args; Pitfall 5 SeedAsync BEFORE RunAsync; Pitfall 8 MapScalarApiReference not UseScalarApiReference)
    - .planning/phases/04-api-layer/04-PATTERNS.md (## Program.cs section showing full composition pattern)
  </read_first>
  <behavior>
    - Program.cs is top-level statements (no Main method) using WebApplication.CreateBuilder(args).
    - Service registrations execute in this exact order: AddControllers, AddProblemDetails, AddExceptionHandler<PersonNotFoundExceptionHandler>, AddExceptionHandler<ValidationExceptionHandler>, AddOpenApi, AddMediator with options.PipelineBehaviors = [typeof(ValidationBehavior<,>)], AddApplication, AddInfrastructure.
    - Middleware pipeline executes in this exact order: UseExceptionHandler() (NO route argument), UseHttpsRedirection, MapControllers, MapOpenApi, MapScalarApiReference.
    - DataSeeder.SeedAsync is awaited on app.Services BEFORE app.RunAsync().
    - The build compiles only after Plan 02 has authored the two exception handler classes; this task uses forward references via using directives — the project will fail to build until Plan 02 lands, so verification here is restricted to scoped checks that do not require a clean build. (See acceptance criteria — verification uses grep + structural assertions, NOT a full dotnet build of the Api project. The full-solution build gate runs after Plan 02 Task 2.)
  </behavior>
  <action>
    Replace the entire contents of src/PersonsAPI.Api/Program.cs with a top-level-statements composition root. Required `using` directives at the top: `using Mediator;`, `using PersonsAPI.Api.ExceptionHandlers;`, `using PersonsAPI.Application;`, `using PersonsAPI.Application.Behaviors;`, `using PersonsAPI.Infrastructure;`, `using PersonsAPI.Infrastructure.Seeder;`, `using Scalar.AspNetCore;`. Body MUST execute these statements in order: (1) `var builder = WebApplication.CreateBuilder(args);` (2) `builder.Services.AddControllers();` (3) `builder.Services.AddProblemDetails();` per D-02 (4) `builder.Services.AddExceptionHandler<PersonNotFoundExceptionHandler>();` per D-01 (5) `builder.Services.AddExceptionHandler<ValidationExceptionHandler>();` per D-01 — order matters, NotFound MUST come first (6) `builder.Services.AddOpenApi();` per DOC-01 (7) `builder.Services.AddMediator(options => options.PipelineBehaviors = new[] { typeof(ValidationBehavior<,>) });` — register Mediator with the ValidationBehavior open generic in its PipelineBehaviors collection (8) `builder.Services.AddApplication();` (9) `builder.Services.AddInfrastructure();` (10) `var app = builder.Build();` (11) `app.UseExceptionHandler();` — NO route string argument per Pitfall 2 (12) `app.UseHttpsRedirection();` (13) `app.MapControllers();` (14) `app.MapOpenApi();` per DOC-01 (15) `app.MapScalarApiReference();` per DOC-02 and Pitfall 8 — MUST use MapScalarApiReference, NEVER UseScalarApiReference (16) `await app.Services.SeedAsync();` per Pitfall 5 — MUST be BEFORE RunAsync (17) `await app.RunAsync();`. Do NOT call AddAuthorization, AddAuthentication, or any auth middleware (out of v1 scope per REQUIREMENTS.md). Do NOT wrap MapOpenApi or MapScalarApiReference in `if (app.Environment.IsDevelopment())` per RESEARCH.md Open Question 2 (learning project — unconditional registration). Do NOT pass a route argument to UseExceptionHandler. Do NOT add a `Mediator.Abstractions` PackageReference to this project — it is transitive via Application. Append a `public partial class Program { }` declaration at the bottom of the file so the test project (Plan 03) can use WebApplicationFactory<Program>. Verify the Program.cs file passes a grep-based structural audit (no build attempted in this task because the referenced exception handler types do not yet exist — that compilation gate is enforced after Plan 02).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f = 'C:/ATS/Git/platform/src/PersonsAPI.Api/Program.cs'; $c = Get-Content $f -Raw; $checks = @( @{n='AddProblemDetails';p='AddProblemDetails\(\)'}, @{n='NotFoundHandler';p='AddExceptionHandler&lt;PersonNotFoundExceptionHandler&gt;\(\)'}, @{n='ValidationHandler';p='AddExceptionHandler&lt;ValidationExceptionHandler&gt;\(\)'}, @{n='AddOpenApi';p='AddOpenApi\(\)'}, @{n='AddMediator';p='AddMediator\('}, @{n='ValidationBehavior';p='typeof\(ValidationBehavior&lt;,&gt;\)'}, @{n='AddApplication';p='AddApplication\(\)'}, @{n='AddInfrastructure';p='AddInfrastructure\(\)'}, @{n='UseExceptionHandler';p='UseExceptionHandler\(\)'}, @{n='MapControllers';p='MapControllers\(\)'}, @{n='MapOpenApi';p='MapOpenApi\(\)'}, @{n='MapScalarApiReference';p='MapScalarApiReference\(\)'}, @{n='SeedAsync';p='SeedAsync\(\)'}, @{n='RunAsync';p='RunAsync\(\)'}, @{n='PartialProgram';p='partial class Program'} ); $fail = $false; foreach ($x in $checks) { if ($c -notmatch $x.p) { Write-Host \"MISSING: $($x.n)\"; $fail = $true } else { Write-Host \"OK: $($x.n)\" } }; if ($c -match 'UseExceptionHandler\(\"') { Write-Host 'FAIL: UseExceptionHandler called with route argument (Pitfall 2)'; $fail = $true }; if ($c -match 'UseScalarApiReference') { Write-Host 'FAIL: UseScalarApiReference used instead of MapScalarApiReference (Pitfall 8)'; $fail = $true }; if ($fail) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `WebApplication.CreateBuilder(args)`.
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `AddProblemDetails()`.
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `AddExceptionHandler<PersonNotFoundExceptionHandler>()` AND `AddExceptionHandler<ValidationExceptionHandler>()`, and the NotFound registration appears BEFORE the Validation registration (line position check).
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `AddOpenApi()` AND `MapOpenApi()`.
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `MapScalarApiReference()` and does NOT contain `UseScalarApiReference` (Pitfall 8).
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `AddMediator(` AND `typeof(ValidationBehavior<,>)` AND `PipelineBehaviors`.
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `AddApplication()` AND `AddInfrastructure()`, and both are called AFTER `AddMediator(`.
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `UseExceptionHandler()` with NO string/route argument inside the parentheses (Pitfall 2).
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `await app.Services.SeedAsync();` and this line appears BEFORE `await app.RunAsync();` (Pitfall 5).
    - Source assertion: src/PersonsAPI.Api/Program.cs contains `public partial class Program` declaration (enables WebApplicationFactory<Program> in Plan 03).
    - Source assertion: src/PersonsAPI.Api/Program.cs middleware ordering: `MapControllers()` appears BEFORE `MapScalarApiReference()` (Pitfall 8 ordering requirement).
    - Behavior assertion: The PowerShell verify command above exits with code 0 (every required token present, no banned tokens).
  </acceptance_criteria>
  <done>Program.cs is a top-level-statements composition root that registers Controllers + ProblemDetails + two IExceptionHandler implementations (NotFound first, Validation second) + OpenApi + Mediator with ValidationBehavior pipeline + Application + Infrastructure; activates UseExceptionHandler with no argument; maps Controllers, OpenApi, and ScalarApiReference; seeds the InMemory store before RunAsync; and exposes `partial class Program` for integration tests in Plan 03. Build verification of the Api project happens after Plan 02 ships the exception handler types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> ASP.NET Core host | Untrusted requests cross here; body and route data are attacker-controlled until validated. |
| Composition root -> Application/Infrastructure DI | Registration order controls whether ValidationBehavior runs; a misregistration could let unvalidated commands reach handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | UseExceptionHandler pipeline | mitigate | Register AddProblemDetails() so unhandled exceptions emit RFC 9457 payloads with no stack trace; Pitfall 2 forbids passing a route to UseExceptionHandler (per D-01/D-02). |
| T-04-01-02 | Tampering | Mediator pipeline | mitigate | Register ValidationBehavior<,> via AddMediator options.PipelineBehaviors so every command/query is validated before reaching the handler — Pitfall 3 forbids a duplicate AddScoped registration. |
| T-04-01-03 | Denial of Service | DataSeeder startup | accept | Seeder runs once on idempotent boot; InMemory provider has bounded data set (3 persons). Low-value attack surface for a learning project per RESEARCH §Security Domain. |
| T-04-01-SC | Tampering | NuGet package installs (Mediator.SourceGenerator, Microsoft.AspNetCore.JsonPatch.SystemTextJson, Microsoft.AspNetCore.OpenApi, Scalar.AspNetCore) | mitigate | All four packages were verified in RESEARCH.md ## Package Legitimacy Audit against nuget.org; pinned exact versions; slopcheck noted as N/A (NuGet ecosystem). No blocking-human checkpoint required because every package is APPROVED (none classified [ASSUMED]/[SUS]/[SLOP]). |
</threat_model>

<verification>
- `dotnet build PersonsAPI.sln` MUST exit 0 after Task 2 (Api project compiles with placeholder Program.cs).
- After Task 3 the Api project will not build until Plan 02 lands the exception handler types — this is expected. The grep-based structural verification in Task 3 gates correctness here.
- The phase-level build/run gate is enforced at the end of Plan 02 (Task 2 verification) and again in Plan 03 (integration smoke test).
- Manual sanity check (optional, not part of automated verification): after Plan 02 completes, `dotnet run --project src/PersonsAPI.Api` must boot, serve `/openapi/v1.json`, and render the Scalar UI at `/scalar`.
</verification>

<success_criteria>
- UpdatePersonDto is a mutable class with four nullable get/set properties; PatchPersonCommand consumers compile unchanged.
- PersonsAPI.Api project exists with Microsoft.NET.Sdk.Web SDK, the four pinned packages, project references to Application and Infrastructure, launch and appsettings files, and is registered in PersonsAPI.sln under the src folder.
- Mediator.SourceGenerator is installed only in the Api project (no leak into Application/Infrastructure/Domain).
- Program.cs is the sole composition root, calls AddProblemDetails + AddExceptionHandler<NotFound> + AddExceptionHandler<Validation> + AddOpenApi + AddMediator(ValidationBehavior pipeline) + AddApplication + AddInfrastructure, then UseExceptionHandler() (no args) + MapControllers + MapOpenApi + MapScalarApiReference, and awaits SeedAsync before RunAsync.
- `partial class Program {}` is declared for WebApplicationFactory<Program> use in Plan 03.
- Full-solution build (`dotnet build PersonsAPI.sln`) succeeds at the end of Task 2; Program.cs structural grep in Task 3 passes; Api-project build will succeed once Plan 02 ships.
</success_criteria>

<output>
Create `.planning/phases/04-api-layer/04-01-SUMMARY.md` when done, summarizing: UpdatePersonDto migration, Api project layout, exact package versions installed, Program.cs registration order, and any deviations from the planned pattern (with rationale).
</output>
