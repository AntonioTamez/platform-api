---
phase: 04-api-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj
  - tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs
  - tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs
  - PersonsAPI.sln
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - DOC-01
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "PersonsAPI.Api.Tests project exists and is registered in PersonsAPI.sln under the tests solution folder"
    - "Integration tests boot the full Api host using WebApplicationFactory<Program> with the InMemory store seeded by DataSeeder"
    - "A test verifies GET /api/persons returns 200 with exactly the three seeded persons (María García López, Carlos Ramírez Martínez, Ana Flores Mendoza)"
    - "A test verifies GET /api/persons/99 returns 404 with application/problem+json and Title=Not Found (ERR-01, ERR-03)"
    - "A test verifies POST /api/persons with an empty body returns 400 application/problem+json with an errors extension dictionary keyed by property name (ERR-01, ERR-02)"
    - "A test verifies POST /api/persons with a valid body returns 201 with a Location header pointing at /api/persons/{newId}"
    - "A test verifies PATCH /api/persons/{id} with a JsonPatchDocument that replaces FirstName returns 200 and the response reflects the patched name"
    - "A test verifies DELETE /api/persons/{id} returns 204 No Content"
    - "A test verifies GET /openapi/v1.json returns 200 with JSON content (DOC-01) and GET /scalar/v1 (or the configured Scalar reference URL) returns 200 with HTML content (DOC-02)"
  artifacts:
    - path: "tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj"
      provides: "xUnit test project hosting Api integration tests"
      contains: "Microsoft.AspNetCore.Mvc.Testing"
    - path: "tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs"
      provides: "End-to-end coverage of the six controller endpoints"
      contains: "WebApplicationFactory<Program>"
    - path: "tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs"
      provides: "ERR-01/02/03 + DOC-01/02 coverage"
      contains: "application/problem+json"
  key_links:
    - from: "tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs"
      to: "src/PersonsAPI.Api/Program.cs"
      via: "WebApplicationFactory<Program> bootstraps the host via the partial Program class"
      pattern: "WebApplicationFactory<Program>"
    - from: "tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs"
      to: "src/PersonsAPI.Api/ExceptionHandlers/PersonNotFoundExceptionHandler.cs"
      via: "GET /api/persons/99 triggers PersonNotFoundException -> 404 Problem Details"
      pattern: "StatusCodes.Status404NotFound"
---

<objective>
Establish a PersonsAPI.Api.Tests integration test project that boots the full ASP.NET Core host with the existing InMemory store seeded by DataSeeder, and prove every ROADMAP success criterion for Phase 4 with executable assertions: six endpoints, Problem Details for 404 and 400, OpenAPI document, and Scalar reference UI.

Purpose: Plans 01 and 02 deliver the production code. This plan locks in behavior with automated tests so future phases (or refactors) cannot regress error handling, route shapes, or documentation availability. Tests are the only mechanical way to verify ERR-01/02/03 + DOC-01/02 at runtime — Plan 01/02 verification gates only check structural source-level correctness and a clean build.

Output: A green xUnit test run executing at minimum nine integration tests against the real Api host, all passing locally with `dotnet test`.
</objective>

<execution_context>
@C:/ATS/Git/platform/.claude/get-shit-done/workflows/execute-plan.md
@C:/ATS/Git/platform/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-api-layer/04-CONTEXT.md
@.planning/phases/04-api-layer/04-RESEARCH.md
@.planning/phases/04-api-layer/04-PATTERNS.md
@.planning/phases/04-api-layer/04-01-PLAN.md
@.planning/phases/04-api-layer/04-02-PLAN.md
@CLAUDE.md
@tests/PersonsAPI.Infrastructure.Tests/PersonsAPI.Infrastructure.Tests.csproj
@src/PersonsAPI.Api/Program.cs
@src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs
@src/PersonsAPI.Application/DTOs/PersonResponse.cs
@src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs
@src/PersonsAPI.Infrastructure/Seeder/DataSeeder.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create PersonsAPI.Api.Tests project, install test packages, register in solution</name>
  <files>tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj, PersonsAPI.sln</files>
  <read_first>
    - tests/PersonsAPI.Infrastructure.Tests/PersonsAPI.Infrastructure.Tests.csproj (analog for test project structure — exact xunit/coverlet/test-sdk versions to mirror)
    - PersonsAPI.sln (current solution file — note the tests folder GUID {0AB3BF05-4346-4AA6-1389-037BE0695223})
    - .planning/phases/04-api-layer/04-PATTERNS.md (## PersonsAPI.Api.Tests.csproj section — full structure, Microsoft.AspNetCore.Mvc.Testing version)
  </read_first>
  <behavior>
    - tests/PersonsAPI.Api.Tests/ exists with a csproj using Microsoft.NET.Sdk SDK (NOT the Web SDK — test projects use the plain SDK).
    - The project references the Api project so WebApplicationFactory<Program> can bootstrap it.
    - Installed packages: coverlet.collector 6.0.4, Microsoft.NET.Test.Sdk 17.14.1, xunit 2.9.3, xunit.runner.visualstudio 3.1.4, AND Microsoft.AspNetCore.Mvc.Testing 10.0.8 (the host-bootstrap helper).
    - IsPackable=false (test project — do not package as NuGet).
    - PersonsAPI.sln contains a new project entry referencing tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj nested under the tests solution folder.
    - `dotnet restore PersonsAPI.sln` and `dotnet build PersonsAPI.sln` both succeed.
  </behavior>
  <action>
    Create directory `tests/PersonsAPI.Api.Tests/`. Create `tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj` with `<Project Sdk="Microsoft.NET.Sdk">` (plain SDK, NOT Web), a PropertyGroup containing `<TargetFramework>net10.0</TargetFramework>`, `<ImplicitUsings>enable</ImplicitUsings>`, `<Nullable>enable</Nullable>`, `<IsPackable>false</IsPackable>`. Add a PackageReference ItemGroup with these exact entries: `coverlet.collector` Version `6.0.4`, `Microsoft.NET.Test.Sdk` Version `17.14.1`, `xunit` Version `2.9.3`, `xunit.runner.visualstudio` Version `3.1.4`, AND `Microsoft.AspNetCore.Mvc.Testing` Version `10.0.8` (the WebApplicationFactory host helper). Add a `<Using Include="Xunit" />` ItemGroup so test files don't need explicit xunit usings. Add a single ProjectReference: `..\..\src\PersonsAPI.Api\PersonsAPI.Api.csproj`. Do NOT reference Application/Infrastructure/Domain directly — they come transitively through the Api reference and tests should drive the system from the HTTP boundary, not internal layers. Register the project in the solution: `dotnet sln C:/ATS/Git/platform/PersonsAPI.sln add C:/ATS/Git/platform/tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj --solution-folder tests`. After this, `dotnet restore PersonsAPI.sln` and `dotnet build PersonsAPI.sln` MUST both succeed (no test files exist yet — empty test project still builds).
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform &amp;&amp; dotnet restore PersonsAPI.sln &amp;&amp; dotnet build PersonsAPI.sln /clp:ErrorsOnly</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj exists and contains `Sdk="Microsoft.NET.Sdk"` (plain SDK).
    - Source assertion: tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj contains `<IsPackable>false</IsPackable>`.
    - Source assertion: tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj contains PackageReference entries for `Microsoft.AspNetCore.Mvc.Testing` Version="10.0.8", `Microsoft.NET.Test.Sdk` Version="17.14.1", `xunit` Version="2.9.3", `xunit.runner.visualstudio` Version="3.1.4", and `coverlet.collector` Version="6.0.4".
    - Source assertion: tests/PersonsAPI.Api.Tests/PersonsAPI.Api.Tests.csproj contains a ProjectReference to `..\..\src\PersonsAPI.Api\PersonsAPI.Api.csproj` and contains NO direct ProjectReference to Application/Infrastructure/Domain.
    - Source assertion: PersonsAPI.sln contains the string `PersonsAPI.Api.Tests` and nests its GUID under the tests folder GUID `{0AB3BF05-4346-4AA6-1389-037BE0695223}`.
    - Behavior assertion: `dotnet restore C:/ATS/Git/platform/PersonsAPI.sln` exits with code 0.
    - Behavior assertion: `dotnet build C:/ATS/Git/platform/PersonsAPI.sln` exits with code 0 and reports 0 errors.
  </acceptance_criteria>
  <done>tests/PersonsAPI.Api.Tests project exists with plain SDK, Microsoft.AspNetCore.Mvc.Testing 10.0.8 installed alongside the standard xunit stack, references the Api project, and is nested under the tests solution folder; the solution restores and builds without errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author PersonsEndpointsTests covering the six endpoints + 201 Location + PATCH semantics</name>
  <files>tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs</files>
  <read_first>
    - src/PersonsAPI.Api/Program.cs (the partial Program class enabling WebApplicationFactory<Program>)
    - src/PersonsAPI.Api/Controllers/PersonsController.cs (six action routes and shapes)
    - src/PersonsAPI.Infrastructure/Seeder/DataSeeder.cs (three seeded persons — María García López, Carlos Ramírez Martínez, Ana Flores Mendoza — driving the count assertion)
    - src/PersonsAPI.Application/DTOs/PersonResponse.cs (response field names for JSON assertions)
    - src/PersonsAPI.Application/DTOs/CreatePersonRequest.cs (POST body shape)
    - src/PersonsAPI.Application/DTOs/UpdatePersonDto.cs (PATCH target — mutable class per Plan 01)
    - .planning/phases/04-api-layer/04-RESEARCH.md (Pattern 4 PATCH; standard assertion shape for 201 + Location)
    - tests/PersonsAPI.Infrastructure.Tests (any .cs file as an example of the test naming/structure convention used in this project)
  </read_first>
  <behavior>
    - The test class uses `IClassFixture<WebApplicationFactory<Program>>` so the host is bootstrapped once and shared across tests.
    - Each test creates a fresh HttpClient via `factory.CreateClient()` and uses `System.Net.Http.Json.HttpClientJsonExtensions` (i.e., `GetFromJsonAsync<T>` / `PostAsJsonAsync<T>`) for JSON I/O.
    - Test "GetAll_ReturnsThreeSeededPersons" asserts: status code 200; deserialized PersonResponse[] has Count == 3 and contains the three seeded first names ("María", "Carlos", "Ana"). Because DataSeeder is idempotent and the InMemory DB persists across tests in the same factory, additional POST-created persons may exist — so the assertion is `>=3` AND contains the three seeded first names (NOT `==3`) to remain robust across test ordering.
    - Test "GetById_KnownId_Returns200WithPerson" asserts: GET /api/persons/1 returns 200 and a PersonResponse with Id==1.
    - Test "GetById_UnknownId_Returns404ProblemDetails" asserts: GET /api/persons/999999 returns 404 with response Content-Type starting with `application/problem+json` and a deserialized ProblemDetails whose Title=="Not Found" and Status==404.
    - Test "Post_ValidBody_Returns201WithLocation" asserts: POST /api/persons with a valid CreatePersonRequest (e.g., FirstName="TestFn", PaternalLastName="TestPa", MaternalLastName="TestMa", DateOfBirth = a valid past date) returns 201; response.Headers.Location is not null AND its absolute path matches `/api/persons/{returnedId}`; the deserialized PersonResponse echoes the FirstName/PaternalLastName/MaternalLastName values and has a non-zero Id.
    - Test "Patch_ReplaceFirstName_Returns200WithUpdatedName" asserts: a PATCH request to a freshly-created person id (created in the same test) using a JsonPatchDocument with a single replace op `{ op: "replace", path: "/firstName", value: "Patched" }` returns 200 and the deserialized PersonResponse.FirstName equals "Patched". The request body MUST set Content-Type `application/json` (the SystemTextJson-based JsonPatch package accepts standard JSON).
    - Test "Delete_KnownId_Returns204" asserts: DELETE /api/persons/{idCreatedInTest} returns 204 No Content; a subsequent GET /api/persons/{sameId} returns 404.
    - All tests live in namespace `PersonsAPI.Api.Tests.Integration`.
    - `dotnet test PersonsAPI.sln` exits 0 with all six tests passing.
  </behavior>
  <action>
    Create directory `tests/PersonsAPI.Api.Tests/Integration/`. Create `tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs` with namespace `PersonsAPI.Api.Tests.Integration`. Required `using` directives: `using System.Net;`, `using System.Net.Http.Headers;`, `using System.Net.Http.Json;`, `using Microsoft.AspNetCore.Mvc.Testing;`, `using Microsoft.AspNetCore.Mvc;` (for ProblemDetails), `using PersonsAPI.Application.DTOs;`. Declare `public sealed class PersonsEndpointsTests(WebApplicationFactory<Program> factory) : IClassFixture<WebApplicationFactory<Program>>`. Add a private helper method `private HttpClient CreateClient() => factory.CreateClient();`. Implement six [Fact] methods with the names listed in <behavior> above. For the PATCH test, construct the JsonPatch request body as a raw string `[{ "op": "replace", "path": "/firstName", "value": "Patched" }]` (camelCase property name to align with default ASP.NET Core JSON serialization conventions) and send it as `new StringContent(body, System.Text.Encoding.UTF8, "application/json")` via `httpClient.PatchAsync($"/api/persons/{newId}", content)`. After PATCH, deserialize the response with `response.Content.ReadFromJsonAsync<PersonResponse>()`. For the 404 test, deserialize the response via `response.Content.ReadFromJsonAsync<ProblemDetails>()` and assert `Title == "Not Found"` and `Status == 404`. For the POST test, use `response.Headers.Location` to assert the Location header was set and contains `/api/persons/`. Do NOT depend on any specific Id values from the seed data — call GET /api/persons first to discover seed Ids if needed, or POST a fresh record at the top of any test that mutates state. Do NOT call `factory.Services.SeedAsync()` manually — Program.cs already does that on host startup. After authoring, run `dotnet test C:/ATS/Git/platform/PersonsAPI.sln --filter "FullyQualifiedName~PersonsEndpointsTests"` and confirm all six tests pass.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform &amp;&amp; dotnet test PersonsAPI.sln --filter "FullyQualifiedName~PersonsEndpointsTests" --nologo --logger "console;verbosity=normal"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains `IClassFixture<WebApplicationFactory<Program>>`.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains exactly six `[Fact]` attributes.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains the strings `"/api/persons"`, `"/api/persons/"`, AND test method names matching the patterns `GetAll_`, `GetById_KnownId_`, `GetById_UnknownId_`, `Post_ValidBody_`, `Patch_ReplaceFirstName_`, `Delete_KnownId_`.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains an assertion that `response.Headers.Location` is not null in the POST test.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains the JsonPatch payload string `"op"` AND `"replace"` AND `"/firstName"`.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/PersonsEndpointsTests.cs contains the seeded first names `"María"`, `"Carlos"`, AND `"Ana"`.
    - Behavior assertion: `dotnet test PersonsAPI.sln --filter "FullyQualifiedName~PersonsEndpointsTests" --nologo` exits 0 with exactly 6 tests passed, 0 failed, 0 skipped.
  </acceptance_criteria>
  <done>PersonsEndpointsTests bootstraps the real host via WebApplicationFactory<Program>, exercises all six endpoints, asserts the 201 Location header for POST, performs a real PATCH replace operation on FirstName, and asserts 204 + subsequent 404 on DELETE — all six tests pass under `dotnet test`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author ProblemDetailsTests covering ERR-01/02/03 + DOC-01/02 at runtime</name>
  <files>tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs</files>
  <read_first>
    - src/PersonsAPI.Api/ExceptionHandlers/PersonNotFoundExceptionHandler.cs (404 shape: about:blank, Not Found, 404, exception Message)
    - src/PersonsAPI.Api/ExceptionHandlers/ValidationExceptionHandler.cs (400 shape: about:blank, Validation Failed, 400, errors dictionary)
    - src/PersonsAPI.Api/Program.cs (MapOpenApi + MapScalarApiReference confirms doc URLs)
    - .planning/phases/04-api-layer/04-CONTEXT.md (D-03 404 shape, D-04 errors dictionary, D-05 detail string)
    - .planning/phases/04-api-layer/04-RESEARCH.md (Open Question 2 — /scalar default route; DOC-02 success criterion confirms /scalar)
  </read_first>
  <behavior>
    - Test "Get_UnknownPerson_Returns404WithRfc9457ProblemDetails" asserts: GET /api/persons/999999 returns 404; Content-Type response header starts with `application/problem+json`; the deserialized ProblemDetails contains Type=="about:blank", Title=="Not Found", Status==404, and Detail contains the substring "Person with ID 999999 was not found." (ERR-01, ERR-03).
    - Test "Post_EmptyBody_Returns400WithErrorsDictionary" asserts: POST /api/persons with body `{}` (all required fields missing) returns 400; Content-Type starts with `application/problem+json`; the deserialized response contains Title=="Validation Failed", Status==400, Detail=="One or more validation errors occurred.", AND an `errors` extension property that is a JSON object whose keys include at minimum the property names FirstName, PaternalLastName, MaternalLastName (case-insensitive contains match) (ERR-01, ERR-02).
    - Test "Get_OpenApiDocument_Returns200Json" asserts: GET /openapi/v1.json returns 200 with response Content-Type starting with `application/json`, and the body contains the substring `"openapi"` (the OpenAPI document version key) AND `"/api/persons"` (the persons path is documented) (DOC-01).
    - Test "Get_ScalarUi_Returns200Html" asserts: GET /scalar/v1 (Scalar's default route under MapScalarApiReference) returns 200 with response Content-Type starting with `text/html`. If that path returns 404, the test falls back to GET /scalar and re-asserts the same — confirming Scalar maps a UI route (DOC-02).
    - All four tests live in namespace `PersonsAPI.Api.Tests.Integration`, use `IClassFixture<WebApplicationFactory<Program>>`, and pass under `dotnet test`.
  </behavior>
  <action>
    Create `tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs` with namespace `PersonsAPI.Api.Tests.Integration`. Required `using` directives: `using System.Net;`, `using System.Net.Http.Json;`, `using System.Text.Json;`, `using Microsoft.AspNetCore.Mvc;`, `using Microsoft.AspNetCore.Mvc.Testing;`. Declare `public sealed class ProblemDetailsTests(WebApplicationFactory<Program> factory) : IClassFixture<WebApplicationFactory<Program>>`. Implement four [Fact] methods named per <behavior>. For the 404 test, after asserting status and content-type, parse the response body as JsonElement (using `await JsonSerializer.DeserializeAsync<JsonDocument>(...)`) to assert `type` == "about:blank", `title` == "Not Found", `status` == 404, and that `detail` contains the substring `Person with ID 999999`. For the 400 test, POST `{}` as `new StringContent("{}", System.Text.Encoding.UTF8, "application/json")`, then parse the response body as JsonDocument and assert: `title` == "Validation Failed", `status` == 400, `detail` == "One or more validation errors occurred.", and that the `errors` JsonElement is `ValueKind == JsonValueKind.Object` with property names whose set INTERSECTED with {"FirstName","PaternalLastName","MaternalLastName"} has at least three elements (case-insensitive). For the OpenAPI test, fetch `/openapi/v1.json` and assert HTTP 200, Content-Type starts with `application/json`, and the raw body string contains `"openapi"` AND `"/api/persons"`. For the Scalar UI test, attempt GET `/scalar/v1` first; if response is non-200, attempt GET `/scalar`; assert at least one of those returns 200 with Content-Type starting with `text/html`. Do NOT hardcode an absolute Scalar URL — read the response URI relative to the test HttpClient base address. After authoring, run `dotnet test C:/ATS/Git/platform/PersonsAPI.sln --filter "FullyQualifiedName~ProblemDetailsTests"` and confirm all four tests pass. Then run the entire test suite `dotnet test PersonsAPI.sln --nologo` to confirm zero regressions in Domain.Tests, Application.Tests, Infrastructure.Tests, AND PersonsAPI.Api.Tests.
  </action>
  <verify>
    <automated>cd C:/ATS/Git/platform &amp;&amp; dotnet test PersonsAPI.sln --nologo --logger "console;verbosity=normal"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs contains `IClassFixture<WebApplicationFactory<Program>>`.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs contains exactly four `[Fact]` attributes.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs contains the strings `application/problem+json`, `"about:blank"`, `"Not Found"`, AND `"Validation Failed"`.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs contains references to `/openapi/v1.json` AND `/scalar` (with or without `/v1` suffix) for DOC-01/02 coverage.
    - Source assertion: tests/PersonsAPI.Api.Tests/Integration/ProblemDetailsTests.cs asserts the response Content-Type for the 404 and 400 tests starts with `application/problem+json` (look for `StartsWith("application/problem+json"` or equivalent).
    - Behavior assertion: `dotnet test C:/ATS/Git/platform/PersonsAPI.sln --filter "FullyQualifiedName~ProblemDetailsTests" --nologo` exits 0 with exactly 4 tests passed.
    - Behavior assertion: `dotnet test C:/ATS/Git/platform/PersonsAPI.sln --nologo` exits 0 with 0 tests failed across ALL test projects (Domain.Tests, Application.Tests, Infrastructure.Tests, PersonsAPI.Api.Tests).
  </acceptance_criteria>
  <done>ProblemDetailsTests proves that ERR-01 (application/problem+json), ERR-02 (errors dictionary on 400), ERR-03 (404 Problem Details for unknown id), DOC-01 (OpenAPI document served), and DOC-02 (Scalar UI served) are all satisfied at runtime; the entire solution test suite passes cleanly with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host -> production Program.cs | Tests run the real composition root via WebApplicationFactory<Program>; a misconfigured test fixture could mask real wiring bugs. |
| Test HttpClient -> /openapi /scalar | Documentation endpoints could leak schema details in a real environment; for this learning project the threat is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | /openapi/v1.json and /scalar | accept | Documented as desirable behavior by DOC-01/DOC-02; no PII, no secrets in the document; ROADMAP success criterion 4 mandates these endpoints. RESEARCH.md Open Question 2 explicitly notes unconditional exposure is acceptable for the learning project. |
| T-04-03-02 | Tampering | Shared InMemory database across tests | mitigate | Each mutation test creates its own person via POST and uses the returned Id rather than depending on seed-data Ids; the GetAll test asserts `>= 3` with seeded-name containment, not strict equality, so concurrent POST-created records don't break tests. |
| T-04-03-SC | Tampering | NuGet test package installs (Microsoft.AspNetCore.Mvc.Testing 10.0.8) | mitigate | Microsoft first-party package, version verified on nuget.org via RESEARCH.md ## Package Legitimacy Audit ecosystem (matches Microsoft.AspNetCore.OpenApi 10.0.8 publisher); no [ASSUMED]/[SUS]/[SLOP] classification. Pinned exact version. |
</threat_model>

<verification>
- `dotnet build PersonsAPI.sln` MUST exit 0 after each task.
- `dotnet test PersonsAPI.sln --nologo` MUST exit 0 at the end of Task 3 with no failed tests across ALL test projects.
- The combined behavioral assertions across Plans 01-03 satisfy every Phase 4 ROADMAP success criterion:
  1. GET 200 list / 200 by-id / 404 unknown id → PersonsEndpointsTests + ProblemDetailsTests
  2. POST 201 + Location / PUT 200 / PATCH 200 / DELETE 204 → PersonsEndpointsTests
  3. 400 application/problem+json with field violations → ProblemDetailsTests
  4. /scalar opens Scalar UI with six endpoints → ProblemDetailsTests + manual smoke optional
</verification>

<success_criteria>
- PersonsAPI.Api.Tests project exists in tests/, references the Api project, includes Microsoft.AspNetCore.Mvc.Testing 10.0.8, and is nested under the tests solution folder in PersonsAPI.sln.
- PersonsEndpointsTests has six [Fact] methods covering GET-all, GET-by-id known and unknown, POST, PATCH, and DELETE; all six pass under `dotnet test`.
- ProblemDetailsTests has four [Fact] methods covering 404 Problem Details, 400 errors dictionary, OpenAPI document availability, and Scalar UI availability; all four pass.
- `dotnet test PersonsAPI.sln --nologo` exits with code 0 and reports zero failed tests across all four test projects.
</success_criteria>

<output>
Create `.planning/phases/04-api-layer/04-03-SUMMARY.md` when done, documenting: the final test project layout, the exact `dotnet test` results (passed/failed/skipped counts per project), how each of the four Phase 4 ROADMAP success criteria is mechanically verified by a named test method, any flakiness observed and how it was resolved, and the resolved Scalar reference URL (/scalar vs /scalar/v1).
</output>
